$defs:
  ActorsModel:
    additionalProperties: false
    properties:
      distributed_objectives:
        $ref: '#/$defs/ToolFeatureModel'
        default:
          source: []
          value: '?'
        description: Per-agent profit maximising / risk minimising objectives can
          be represented within the larger system-level optimisation (e.g. bidding
          in day-ahead / intra-day markets, behind-the-meter prosumer decisions)
      equilibrium:
        $ref: '#/$defs/ToolFeatureModel'
        default:
          source: []
          value: '?'
        description: Market equilibrium found across actors with competing objectives.
          This could be applied via complementarity/MCP/bi-level formulations or a
          quadratic-markup reformulation of the cost-minimisation objective (e.g.
          Cournot-Nash oligopoly modelling).
      portfolio:
        $ref: '#/$defs/ToolFeatureModel'
        default:
          source: []
          value: '?'
        description: Agent asset portfolios and associated constraints and cost considerations
          (e.g. limited cross-portfolio capital investment, risk appetite).
    title: ActorsModel
    type: object
  AssetCapacityRepresentationModel:
    additionalProperties: false
    properties:
      continuous_capacity:
        $ref: '#/$defs/ToolFeatureModel'
        default:
          source: []
          value: '?'
        description: Asset capacity is fully endogenous and modeled as a continuous
          (linear) decision variable. An optional maximum capacity can be specified
          in the input data (e.g. PV capacity that can be installed at any capacity
          from zero to a maximum defined by land-use constraints).
      exogenous_capacity:
        $ref: '#/$defs/ToolFeatureModel'
        default:
          source: []
          value: '?'
        description: Asset capacity is fixed and fully defined in the input data (e.g.
          for modelling dispatch only).
      integer_investment_continuous_capacity:
        $ref: '#/$defs/ToolFeatureModel'
        default:
          source: []
          value: '?'
        description: Asset capacity magnitude is endogenous and modeled as a continuous
          (linear) decision variable. Investment is also endogenous and represented
          by a binary decision variable. An optional maximum capacity can be specified
          in the input data (e.g. gas turbines that you want to track as individual
          units and whose capacity is not fixed by the manufacturer).
      integer_investment_exogenous_capacity:
        $ref: '#/$defs/ToolFeatureModel'
        default:
          source: []
          value: '?'
        description: Asset capacity magnitude is fixed in the input data. Investment
          is modeled as a discrete (i.e., integer) decision variable (e.g. nuclear
          power plants whose capacity is fixed & indivisible due to external constraints
          but the decision to invest in this capacity is as-yet undecided).
    title: AssetCapacityRepresentationModel
    type: object
  AssetCharacteristicsConfigurableDimensionsFeatureModel:
    additionalProperties: false
    properties:
      source:
        description: 'Link(s) to source to validate the given value.

          This is usually used to validate a `y` (documentation link) or `dev` (issue
          or pull request link) but can feasibly be used to validate an `n`.'
        items:
          anyOf:
          - format: uri
            maxLength: 2083
            minLength: 1
            type: string
          - additionalProperties:
              format: uri
              maxLength: 2083
              minLength: 1
              type: string
            maxProperties: 1
            minProperties: 1
            propertyNames:
              enum:
              - investment
              - operation
            type: object
        title: Source
        type: array
      value:
        $ref: '#/$defs/AssetCharacteristicsConfigurableDimensionsValueModel'
        default:
          investment: '?'
          operation: '?'
        description: Per-slice feature value.
    title: AssetCharacteristicsConfigurableDimensionsFeatureModel
    type: object
  AssetCharacteristicsConfigurableDimensionsValueModel:
    additionalProperties: false
    properties:
      investment:
        default: '?'
        description: Applies to investment characteristics (e.g. capital cost, maximum
          buildable capacity).
        enum:
        - y
        - n
        - dev
        - '?'
        title: Investment
        type: string
      operation:
        default: '?'
        description: Applies to operating characteristics (e.g. efficiency, resource
          availability).
        enum:
        - y
        - n
        - dev
        - '?'
        title: Operation
        type: string
    title: AssetCharacteristicsConfigurableDimensionsValueModel
    type: object
  AssetCharacteristicsExtensibleDimensionsFeatureModel:
    additionalProperties: false
    properties:
      source:
        description: 'Link(s) to source to validate the given value.

          This is usually used to validate a `y` (documentation link) or `dev` (issue
          or pull request link) but can feasibly be used to validate an `n`.'
        items:
          anyOf:
          - format: uri
            maxLength: 2083
            minLength: 1
            type: string
          - additionalProperties:
              format: uri
              maxLength: 2083
              minLength: 1
              type: string
            maxProperties: 1
            minProperties: 1
            propertyNames:
              enum:
              - investment
              - operation
            type: object
        title: Source
        type: array
      value:
        $ref: '#/$defs/AssetCharacteristicsExtensibleDimensionsValueModel'
        default:
          investment: '?'
          operation: '?'
        description: Per-slice feature value.
    title: AssetCharacteristicsExtensibleDimensionsFeatureModel
    type: object
  AssetCharacteristicsExtensibleDimensionsValueModel:
    additionalProperties: false
    properties:
      investment:
        default: '?'
        description: Applies to investment characteristics.
        enum:
        - y
        - n
        - dev
        - '?'
        title: Investment
        type: string
      operation:
        default: '?'
        description: Applies to operating characteristics.
        enum:
        - y
        - n
        - dev
        - '?'
        title: Operation
        type: string
    title: AssetCharacteristicsExtensibleDimensionsValueModel
    type: object
  AssetCharacteristicsLinearFeatureModel:
    additionalProperties: false
    properties:
      source:
        description: 'Link(s) to source to validate the given value.

          This is usually used to validate a `y` (documentation link) or `dev` (issue
          or pull request link) but can feasibly be used to validate an `n`.'
        items:
          anyOf:
          - format: uri
            maxLength: 2083
            minLength: 1
            type: string
          - additionalProperties:
              format: uri
              maxLength: 2083
              minLength: 1
              type: string
            maxProperties: 1
            minProperties: 1
            propertyNames:
              enum:
              - investment
              - operation
            type: object
        title: Source
        type: array
      value:
        $ref: '#/$defs/AssetCharacteristicsLinearValueModel'
        default:
          investment: '?'
          operation: '?'
        description: Per-slice feature value.
    title: AssetCharacteristicsLinearFeatureModel
    type: object
  AssetCharacteristicsLinearValueModel:
    additionalProperties: false
    properties:
      investment:
        default: '?'
        description: Varies linearly with an investment decision variable (e.g. resource
          availability increasing with asset capacity).
        enum:
        - y
        - n
        - dev
        - '?'
        title: Investment
        type: string
      operation:
        default: '?'
        description: Varies linearly with an operating decision variable.
        enum:
        - y
        - n
        - dev
        - '?'
        title: Operation
        type: string
    title: AssetCharacteristicsLinearValueModel
    type: object
  AssetCharacteristicsModel:
    additionalProperties: false
    properties:
      configurable_dimensions:
        $ref: '#/$defs/AssetCharacteristicsConfigurableDimensionsFeatureModel'
        default:
          source: []
          value:
            investment: '?'
            operation: '?'
        description: A characteristic's dimensionality can be chosen by the user per
          characteristic, rather than being fixed by the tool for each named parameter
          (Full user choice on whether e.g. parameters vary by time, space, both,
          or neither).
      extensible_dimensions:
        $ref: '#/$defs/AssetCharacteristicsExtensibleDimensionsFeatureModel'
        default:
          source: []
          value:
            investment: '?'
            operation: '?'
        description: A dimension not natively defined by the tool (e.g. a custom vintage,
          cohort, or scenario axis) can be introduced by the user. The tool's mathematical
          formulation can either handle this automatically or requires custom math
          (see `interface__math.user_defined_math`).
      linear:
        $ref: '#/$defs/AssetCharacteristicsLinearFeatureModel'
        default:
          source: []
          value:
            investment: '?'
            operation: '?'
        description: Asset characteristics vary linearly with a single decision variable.
      nonlinear:
        $ref: '#/$defs/AssetCharacteristicsNonlinearFeatureModel'
        default:
          source: []
          value:
            investment: '?'
            operation: '?'
        description: Asset characteristics link together several decision variables.
          This creates a non-linear relationship that can be described by e.g. a piecewise
          linear curve or through iterative solving.
      path_dependent:
        $ref: '#/$defs/AssetCharacteristicsPathDependentFeatureModel'
        default:
          source: []
          value:
            investment: '?'
            operation: '?'
        description: Asset characteristics evolve based on cumulative historical conditions.
      quadratic:
        $ref: '#/$defs/AssetCharacteristicsQuadraticFeatureModel'
        default:
          source: []
          value:
            investment: '?'
            operation: '?'
        description: Asset characteristics vary quadratically with a decision variable,
          passed natively to a QP/QCP-capable solver.
    title: AssetCharacteristicsModel
    type: object
  AssetCharacteristicsNonlinearFeatureModel:
    additionalProperties: false
    properties:
      source:
        description: 'Link(s) to source to validate the given value.

          This is usually used to validate a `y` (documentation link) or `dev` (issue
          or pull request link) but can feasibly be used to validate an `n`.'
        items:
          anyOf:
          - format: uri
            maxLength: 2083
            minLength: 1
            type: string
          - additionalProperties:
              format: uri
              maxLength: 2083
              minLength: 1
              type: string
            maxProperties: 1
            minProperties: 1
            propertyNames:
              enum:
              - investment
              - operation
            type: object
        title: Source
        type: array
      value:
        $ref: '#/$defs/AssetCharacteristicsNonlinearValueModel'
        default:
          investment: '?'
          operation: '?'
        description: Per-slice feature value.
    title: AssetCharacteristicsNonlinearFeatureModel
    type: object
  AssetCharacteristicsNonlinearValueModel:
    additionalProperties: false
    properties:
      investment:
        default: '?'
        description: Varies with an investment decision variable (e.g. storage head
          effects, pressurisation energy curves linked to capacity).
        enum:
        - y
        - n
        - dev
        - '?'
        title: Investment
        type: string
      operation:
        default: '?'
        description: Varies with an operating decision variable (e.g., part-load efficiency,
          line losses).
        enum:
        - y
        - n
        - dev
        - '?'
        title: Operation
        type: string
    title: AssetCharacteristicsNonlinearValueModel
    type: object
  AssetCharacteristicsPathDependentFeatureModel:
    additionalProperties: false
    properties:
      source:
        description: 'Link(s) to source to validate the given value.

          This is usually used to validate a `y` (documentation link) or `dev` (issue
          or pull request link) but can feasibly be used to validate an `n`.'
        items:
          anyOf:
          - format: uri
            maxLength: 2083
            minLength: 1
            type: string
          - additionalProperties:
              format: uri
              maxLength: 2083
              minLength: 1
              type: string
            maxProperties: 1
            minProperties: 1
            propertyNames:
              enum:
              - investment
              - operation
            type: object
        title: Source
        type: array
      value:
        $ref: '#/$defs/AssetCharacteristicsPathDependentValueModel'
        default:
          investment: '?'
          operation: '?'
        description: Per-slice feature value.
    title: AssetCharacteristicsPathDependentFeatureModel
    type: object
  AssetCharacteristicsPathDependentValueModel:
    additionalProperties: false
    properties:
      investment:
        default: '?'
        description: Investment characteristics evolve based on investments made in
          previous periods (e.g. learning curves).
        enum:
        - y
        - n
        - dev
        - '?'
        title: Investment
        type: string
      operation:
        default: '?'
        description: Operational characteristics evolve based on operation in previous
          timesteps (e.g. battery cycling/calendar degradation, filter fouling).
        enum:
        - y
        - n
        - dev
        - '?'
        title: Operation
        type: string
    title: AssetCharacteristicsPathDependentValueModel
    type: object
  AssetCharacteristicsQuadraticFeatureModel:
    additionalProperties: false
    properties:
      source:
        description: 'Link(s) to source to validate the given value.

          This is usually used to validate a `y` (documentation link) or `dev` (issue
          or pull request link) but can feasibly be used to validate an `n`.'
        items:
          anyOf:
          - format: uri
            maxLength: 2083
            minLength: 1
            type: string
          - additionalProperties:
              format: uri
              maxLength: 2083
              minLength: 1
              type: string
            maxProperties: 1
            minProperties: 1
            propertyNames:
              enum:
              - investment
              - operation
            type: object
        title: Source
        type: array
      value:
        $ref: '#/$defs/AssetCharacteristicsQuadraticValueModel'
        default:
          investment: '?'
          operation: '?'
        description: Per-slice feature value.
    title: AssetCharacteristicsQuadraticFeatureModel
    type: object
  AssetCharacteristicsQuadraticValueModel:
    additionalProperties: false
    properties:
      investment:
        default: '?'
        description: Varies quadratically with an investment decision variable.
        enum:
        - y
        - n
        - dev
        - '?'
        title: Investment
        type: string
      operation:
        default: '?'
        description: Varies quadratically with an operating decision variable (e.g.
          quadratic fuel curves, loss approximations).
        enum:
        - y
        - n
        - dev
        - '?'
        title: Operation
        type: string
    title: AssetCharacteristicsQuadraticValueModel
    type: object
  AssetConstraintsBoundaryConditionsFeatureModel:
    additionalProperties: false
    properties:
      source:
        description: 'Link(s) to source to validate the given value.

          This is usually used to validate a `y` (documentation link) or `dev` (issue
          or pull request link) but can feasibly be used to validate an `n`.'
        items:
          anyOf:
          - format: uri
            maxLength: 2083
            minLength: 1
            type: string
          - additionalProperties:
              format: uri
              maxLength: 2083
              minLength: 1
              type: string
            maxProperties: 1
            minProperties: 1
            propertyNames:
              enum:
              - investment
              - operation
            type: object
        title: Source
        type: array
      value:
        $ref: '#/$defs/AssetConstraintsBoundaryConditionsValueModel'
        default:
          investment: '?'
          operation: '?'
        description: Per-slice feature value.
    title: AssetConstraintsBoundaryConditionsFeatureModel
    type: object
  AssetConstraintsBoundaryConditionsValueModel:
    additionalProperties: false
    properties:
      investment:
        default: '?'
        description: Investment optimisation can start from an existing asset stock
          rather than from an empty (greenfield) system or can define a desired end-state.
          This allows pre-existing capacities to be represented (initial) and for
          backcasting system design (end-state).
        enum:
        - y
        - n
        - dev
        - '?'
        title: Investment
        type: string
      operation:
        default: '?'
        description: Initial or end-state operational conditions can be set (e.g.
          start or end storage level requirements).
        enum:
        - y
        - n
        - dev
        - '?'
        title: Operation
        type: string
    title: AssetConstraintsBoundaryConditionsValueModel
    type: object
  AssetConstraintsCommitmentFeatureModel:
    additionalProperties: false
    properties:
      source:
        description: 'Link(s) to source to validate the given value.

          This is usually used to validate a `y` (documentation link) or `dev` (issue
          or pull request link) but can feasibly be used to validate an `n`.'
        items:
          anyOf:
          - format: uri
            maxLength: 2083
            minLength: 1
            type: string
          - additionalProperties:
              format: uri
              maxLength: 2083
              minLength: 1
              type: string
            maxProperties: 1
            minProperties: 1
            propertyNames:
              enum:
              - investment
              - operation
            type: object
        title: Source
        type: array
      value:
        $ref: '#/$defs/AssetConstraintsCommitmentValueModel'
        default:
          investment: '?'
          operation: '?'
        description: Per-slice feature value.
    title: AssetConstraintsCommitmentFeatureModel
    type: object
  AssetConstraintsCommitmentValueModel:
    additionalProperties: false
    properties:
      investment:
        default: '?'
        description: Limits on investment unit purchase within investment periods.
          Usually requires `asset__capacity_representation.integer_investment_...`.
        enum:
        - y
        - n
        - dev
        - '?'
        title: Investment
        type: string
      operation:
        default: '?'
        description: Limits on operational unit commit within timesteps (e.g., forced/planned
          outage windows). Usually requires `asset__operating_representation.unit_commitment`.
        enum:
        - y
        - n
        - dev
        - '?'
        title: Operation
        type: string
    title: AssetConstraintsCommitmentValueModel
    type: object
  AssetConstraintsInterAssetFeatureModel:
    additionalProperties: false
    properties:
      source:
        description: 'Link(s) to source to validate the given value.

          This is usually used to validate a `y` (documentation link) or `dev` (issue
          or pull request link) but can feasibly be used to validate an `n`.'
        items:
          anyOf:
          - format: uri
            maxLength: 2083
            minLength: 1
            type: string
          - additionalProperties:
              format: uri
              maxLength: 2083
              minLength: 1
              type: string
            maxProperties: 1
            minProperties: 1
            propertyNames:
              enum:
              - investment
              - operation
            type: object
        title: Source
        type: array
      value:
        $ref: '#/$defs/AssetConstraintsInterAssetValueModel'
        default:
          investment: '?'
          operation: '?'
        description: Per-slice feature value.
    title: AssetConstraintsInterAssetFeatureModel
    type: object
  AssetConstraintsInterAssetValueModel:
    additionalProperties: false
    properties:
      investment:
        default: '?'
        description: Investment limits that link assets (e.g., land-use competition,
          system-wide embodied emissions limits).
        enum:
        - y
        - n
        - dev
        - '?'
        title: Investment
        type: string
      operation:
        default: '?'
        description: Operating limits that link assets (e.g., regional emissions limits
          applied across all emitting assets).
        enum:
        - y
        - n
        - dev
        - '?'
        title: Operation
        type: string
    title: AssetConstraintsInterAssetValueModel
    type: object
  AssetConstraintsInterSpatialFeatureModel:
    additionalProperties: false
    properties:
      source:
        description: 'Link(s) to source to validate the given value.

          This is usually used to validate a `y` (documentation link) or `dev` (issue
          or pull request link) but can feasibly be used to validate an `n`.'
        items:
          anyOf:
          - format: uri
            maxLength: 2083
            minLength: 1
            type: string
          - additionalProperties:
              format: uri
              maxLength: 2083
              minLength: 1
              type: string
            maxProperties: 1
            minProperties: 1
            propertyNames:
              enum:
              - investment
              - operation
            type: object
        title: Source
        type: array
      value:
        $ref: '#/$defs/AssetConstraintsInterSpatialValueModel'
        default:
          investment: '?'
          operation: '?'
        description: Per-slice feature value.
    title: AssetConstraintsInterSpatialFeatureModel
    type: object
  AssetConstraintsInterSpatialValueModel:
    additionalProperties: false
    properties:
      investment:
        default: '?'
        description: Investment limits that link geographic regions.
        enum:
        - y
        - n
        - dev
        - '?'
        title: Investment
        type: string
      operation:
        default: '?'
        description: Operating limits that link geographic regions.
        enum:
        - y
        - n
        - dev
        - '?'
        title: Operation
        type: string
    title: AssetConstraintsInterSpatialValueModel
    type: object
  AssetConstraintsInterTemporalFeatureModel:
    additionalProperties: false
    properties:
      source:
        description: 'Link(s) to source to validate the given value.

          This is usually used to validate a `y` (documentation link) or `dev` (issue
          or pull request link) but can feasibly be used to validate an `n`.'
        items:
          anyOf:
          - format: uri
            maxLength: 2083
            minLength: 1
            type: string
          - additionalProperties:
              format: uri
              maxLength: 2083
              minLength: 1
              type: string
            maxProperties: 1
            minProperties: 1
            propertyNames:
              enum:
              - investment
              - operation
            type: object
        title: Source
        type: array
      value:
        $ref: '#/$defs/AssetConstraintsInterTemporalValueModel'
        default:
          investment: '?'
          operation: '?'
        description: Per-slice feature value.
    title: AssetConstraintsInterTemporalFeatureModel
    type: object
  AssetConstraintsInterTemporalValueModel:
    additionalProperties: false
    properties:
      investment:
        default: '?'
        description: Investment limits linking investment periods (e.g. build-rate
          limits, phased retirement, availability windows).
        enum:
        - y
        - n
        - dev
        - '?'
        title: Investment
        type: string
      operation:
        default: '?'
        description: Operating limits that link time periods (e.g., ramping, run up,
          load shifting).
        enum:
        - y
        - n
        - dev
        - '?'
        title: Operation
        type: string
    title: AssetConstraintsInterTemporalValueModel
    type: object
  AssetConstraintsIntraTemporalFeatureModel:
    additionalProperties: false
    properties:
      source:
        description: 'Link(s) to source to validate the given value.

          This is usually used to validate a `y` (documentation link) or `dev` (issue
          or pull request link) but can feasibly be used to validate an `n`.'
        items:
          anyOf:
          - format: uri
            maxLength: 2083
            minLength: 1
            type: string
          - additionalProperties:
              format: uri
              maxLength: 2083
              minLength: 1
              type: string
            maxProperties: 1
            minProperties: 1
            propertyNames:
              enum:
              - investment
              - operation
            type: object
        title: Source
        type: array
      value:
        $ref: '#/$defs/AssetConstraintsIntraTemporalValueModel'
        default:
          investment: '?'
          operation: '?'
        description: Per-slice feature value.
    title: AssetConstraintsIntraTemporalFeatureModel
    type: object
  AssetConstraintsIntraTemporalValueModel:
    additionalProperties: false
    properties:
      investment:
        default: '?'
        description: Limits on investment within investment periods (e.g. minimum/maximum
          size).
        enum:
        - y
        - n
        - dev
        - '?'
        title: Investment
        type: string
      operation:
        default: '?'
        description: Limits on operation within timesteps (e.g. capacity de-rating
          profiles, minimum/maximum load levels, renewables capacity factors).
        enum:
        - y
        - n
        - dev
        - '?'
        title: Operation
        type: string
    title: AssetConstraintsIntraTemporalValueModel
    type: object
  AssetConstraintsModel:
    additionalProperties: false
    properties:
      boundary_conditions:
        $ref: '#/$defs/AssetConstraintsBoundaryConditionsFeatureModel'
        default:
          source: []
          value:
            investment: '?'
            operation: '?'
        description: Initial and end-state conditions can be set.
      commitment:
        $ref: '#/$defs/AssetConstraintsCommitmentFeatureModel'
        default:
          source: []
          value:
            investment: '?'
            operation: '?'
        description: Limits on commitment to investing in or operating an asset in
          a given time period.
      dedicated_flow_routing:
        $ref: '#/$defs/ToolFeatureModel'
        default:
          source: []
          value: '?'
        description: Carrier out-flow from one asset can be reserved as in-flow to
          a specific subset of other assets (e.g. fuel stockpiling ahead of conversion,
          hydropower cascades, CHP internal steam flows).
      inter_asset:
        $ref: '#/$defs/AssetConstraintsInterAssetFeatureModel'
        default:
          source: []
          value:
            investment: '?'
            operation: '?'
        description: Limits that link assets, within or across regions and time.
      inter_spatial:
        $ref: '#/$defs/AssetConstraintsInterSpatialFeatureModel'
        default:
          source: []
          value:
            investment: '?'
            operation: '?'
        description: Limits that link geographic regions (e.g. to ensure regional
          equity).
      inter_temporal:
        $ref: '#/$defs/AssetConstraintsInterTemporalFeatureModel'
        default:
          source: []
          value:
            investment: '?'
            operation: '?'
        description: Limits that link across time periods.
      intra_temporal:
        $ref: '#/$defs/AssetConstraintsIntraTemporalFeatureModel'
        default:
          source: []
          value:
            investment: '?'
            operation: '?'
        description: Limits on how much assets can output in a given time period
      total:
        $ref: '#/$defs/AssetConstraintsTotalFeatureModel'
        default:
          source: []
          value:
            investment: '?'
            operation: '?'
        description: Limit on an aggregate quantity over a subset or the entire time
          horizon / spatial scope. This can represent real contract arrangements /
          regulation.
    title: AssetConstraintsModel
    type: object
  AssetConstraintsTotalFeatureModel:
    additionalProperties: false
    properties:
      source:
        description: 'Link(s) to source to validate the given value.

          This is usually used to validate a `y` (documentation link) or `dev` (issue
          or pull request link) but can feasibly be used to validate an `n`.'
        items:
          anyOf:
          - format: uri
            maxLength: 2083
            minLength: 1
            type: string
          - additionalProperties:
              format: uri
              maxLength: 2083
              minLength: 1
              type: string
            maxProperties: 1
            minProperties: 1
            propertyNames:
              enum:
              - investment
              - operation
            type: object
        title: Source
        type: array
      value:
        $ref: '#/$defs/AssetConstraintsTotalValueModel'
        default:
          investment: '?'
          operation: '?'
        description: Per-slice feature value.
    title: AssetConstraintsTotalFeatureModel
    type: object
  AssetConstraintsTotalValueModel:
    additionalProperties: false
    properties:
      investment:
        default: '?'
        description: Limit on aggregate investment (e.g. policy targets).
        enum:
        - y
        - n
        - dev
        - '?'
        title: Investment
        type: string
      operation:
        default: '?'
        description: Limit on total asset carrier in-/out-flow (e.g. limits on storage
          cycling, annual throughput limits, uptime).
        enum:
        - y
        - n
        - dev
        - '?'
        title: Operation
        type: string
    title: AssetConstraintsTotalValueModel
    type: object
  AssetCostInterTemporalFeatureModel:
    additionalProperties: false
    properties:
      source:
        description: 'Link(s) to source to validate the given value.

          This is usually used to validate a `y` (documentation link) or `dev` (issue
          or pull request link) but can feasibly be used to validate an `n`.'
        items:
          anyOf:
          - format: uri
            maxLength: 2083
            minLength: 1
            type: string
          - additionalProperties:
              format: uri
              maxLength: 2083
              minLength: 1
              type: string
            maxProperties: 1
            minProperties: 1
            propertyNames:
              enum:
              - investment
              - operation
            type: object
        title: Source
        type: array
      value:
        $ref: '#/$defs/AssetCostInterTemporalValueModel'
        default:
          investment: '?'
          operation: '?'
        description: Per-slice feature value.
    title: AssetCostInterTemporalFeatureModel
    type: object
  AssetCostInterTemporalValueModel:
    additionalProperties: false
    properties:
      investment:
        default: '?'
        description: Costs applied between investment periods (e.g. learning rate
          penalty).
        enum:
        - y
        - n
        - dev
        - '?'
        title: Investment
        type: string
      operation:
        default: '?'
        description: Costs applied between timesteps (e.g. ramping wear cost).
        enum:
        - y
        - n
        - dev
        - '?'
        title: Operation
        type: string
    title: AssetCostInterTemporalValueModel
    type: object
  AssetCostLinearFeatureModel:
    additionalProperties: false
    properties:
      source:
        description: 'Link(s) to source to validate the given value.

          This is usually used to validate a `y` (documentation link) or `dev` (issue
          or pull request link) but can feasibly be used to validate an `n`.'
        items:
          anyOf:
          - format: uri
            maxLength: 2083
            minLength: 1
            type: string
          - additionalProperties:
              format: uri
              maxLength: 2083
              minLength: 1
              type: string
            maxProperties: 1
            minProperties: 1
            propertyNames:
              enum:
              - investment
              - operation
            type: object
        title: Source
        type: array
      value:
        $ref: '#/$defs/AssetCostLinearValueModel'
        default:
          investment: '?'
          operation: '?'
        description: Per-slice feature value.
    title: AssetCostLinearFeatureModel
    type: object
  AssetCostLinearValueModel:
    additionalProperties: false
    properties:
      investment:
        default: '?'
        description: Capital cost which varies linearly with asset nameplate capacity
          (e.g. EUR/MW).
        enum:
        - y
        - n
        - dev
        - '?'
        title: Investment
        type: string
      operation:
        default: '?'
        description: Operating cost which varies linearly with unit in-/out-flow (e.g.
          EUR/MWh).
        enum:
        - y
        - n
        - dev
        - '?'
        title: Operation
        type: string
    title: AssetCostLinearValueModel
    type: object
  AssetCostModel:
    additionalProperties: false
    properties:
      annuitisation:
        $ref: '#/$defs/ToolFeatureModel'
        default:
          source: []
          value: '?'
        description: An asset's capital investment can be annuitised to account for
          factors linked to its expected economic life and financing arrangements.
          (e.g., weighted cost of capital, inflation and tax rates).
      inter_temporal:
        $ref: '#/$defs/AssetCostInterTemporalFeatureModel'
        default:
          source: []
          value:
            investment: '?'
            operation: '?'
        description: Costs applied to the magnitude of change in a decision variable
          between time periods.
      linear:
        $ref: '#/$defs/AssetCostLinearFeatureModel'
        default:
          source: []
          value:
            investment: '?'
            operation: '?'
        description: Cost which varies linearly with a decision variable.
      nonlinear:
        $ref: '#/$defs/AssetCostNonlinearFeatureModel'
        default:
          source: []
          value:
            investment: '?'
            operation: '?'
        description: Cost which varies non-linearly, usually described in the objective
          using piecewise linear curves.
      threshold_deviation:
        $ref: '#/$defs/AssetCostThresholdDeviationFeatureModel'
        default:
          source: []
          value:
            investment: '?'
            operation: '?'
        description: Costs on deviations above/below a contracted or reference level.
          This is applied as a soft constraint requiring a reference level plus deviation
          variables.
      unit:
        $ref: '#/$defs/AssetCostUnitFeatureModel'
        default:
          source: []
          value:
            investment: '?'
            operation: '?'
        description: Cost which is incurred as a fixed lump sum, irrespective of magnitude.
    title: AssetCostModel
    type: object
  AssetCostNonlinearFeatureModel:
    additionalProperties: false
    properties:
      source:
        description: 'Link(s) to source to validate the given value.

          This is usually used to validate a `y` (documentation link) or `dev` (issue
          or pull request link) but can feasibly be used to validate an `n`.'
        items:
          anyOf:
          - format: uri
            maxLength: 2083
            minLength: 1
            type: string
          - additionalProperties:
              format: uri
              maxLength: 2083
              minLength: 1
              type: string
            maxProperties: 1
            minProperties: 1
            propertyNames:
              enum:
              - investment
              - operation
            type: object
        title: Source
        type: array
      value:
        $ref: '#/$defs/AssetCostNonlinearValueModel'
        default:
          investment: '?'
          operation: '?'
        description: Per-slice feature value.
    title: AssetCostNonlinearFeatureModel
    type: object
  AssetCostNonlinearValueModel:
    additionalProperties: false
    properties:
      investment:
        default: '?'
        description: Capital cost economies of scale, where cost per capacity decreases
          with increasing capacity. In some cases, it is possible to describe this
          without an explicit piecewise constraint. Instead, it may be approximated
          by multiple assets of the same class (e.g. "wind turbine") that each have
          different costs and maximum capacities.
        enum:
        - y
        - n
        - dev
        - '?'
        title: Investment
        type: string
      operation:
        default: '?'
        description: Operating cost generation bid increment / decrement levels.
        enum:
        - y
        - n
        - dev
        - '?'
        title: Operation
        type: string
    title: AssetCostNonlinearValueModel
    type: object
  AssetCostThresholdDeviationFeatureModel:
    additionalProperties: false
    properties:
      source:
        description: 'Link(s) to source to validate the given value.

          This is usually used to validate a `y` (documentation link) or `dev` (issue
          or pull request link) but can feasibly be used to validate an `n`.'
        items:
          anyOf:
          - format: uri
            maxLength: 2083
            minLength: 1
            type: string
          - additionalProperties:
              format: uri
              maxLength: 2083
              minLength: 1
              type: string
            maxProperties: 1
            minProperties: 1
            propertyNames:
              enum:
              - investment
              - operation
            type: object
        title: Source
        type: array
      value:
        $ref: '#/$defs/AssetCostThresholdDeviationValueModel'
        default:
          investment: '?'
          operation: '?'
        description: Per-slice feature value.
    title: AssetCostThresholdDeviationFeatureModel
    type: object
  AssetCostThresholdDeviationValueModel:
    additionalProperties: false
    properties:
      investment:
        default: '?'
        description: Deviation from a contracted or reference investment level (e.g.
          rebate for providing firm capacity, penalty for exceeding land-use limits).
        enum:
        - y
        - n
        - dev
        - '?'
        title: Investment
        type: string
      operation:
        default: '?'
        description: Deviation from a contracted or reference operating level (e.g.
          feed-in tariffs, redispatch cost relative to contracted dispatch).
        enum:
        - y
        - n
        - dev
        - '?'
        title: Operation
        type: string
    title: AssetCostThresholdDeviationValueModel
    type: object
  AssetCostUnitFeatureModel:
    additionalProperties: false
    properties:
      source:
        description: 'Link(s) to source to validate the given value.

          This is usually used to validate a `y` (documentation link) or `dev` (issue
          or pull request link) but can feasibly be used to validate an `n`.'
        items:
          anyOf:
          - format: uri
            maxLength: 2083
            minLength: 1
            type: string
          - additionalProperties:
              format: uri
              maxLength: 2083
              minLength: 1
              type: string
            maxProperties: 1
            minProperties: 1
            propertyNames:
              enum:
              - investment
              - operation
            type: object
        title: Source
        type: array
      value:
        $ref: '#/$defs/AssetCostUnitValueModel'
        default:
          investment: '?'
          operation: '?'
        description: Per-slice feature value.
    title: AssetCostUnitFeatureModel
    type: object
  AssetCostUnitValueModel:
    additionalProperties: false
    properties:
      investment:
        default: '?'
        description: Initial capital cost incurred on asset purchase, irrespective
          of capacity (e.g. land purchase, design phase).
        enum:
        - y
        - n
        - dev
        - '?'
        title: Investment
        type: string
      operation:
        default: '?'
        description: Operating cost incurred on unit commitment (e.g. start-up costs).
        enum:
        - y
        - n
        - dev
        - '?'
        title: Operation
        type: string
    title: AssetCostUnitValueModel
    type: object
  AssetOperatingRepresentationModel:
    additionalProperties: false
    properties:
      maintenance_scheduling:
        $ref: '#/$defs/ToolFeatureModel'
        default:
          source: []
          value: '?'
        description: Asset maintenance / outage schedules are derived endogenously,
          co-optimised with operation. Exogenously fixed maintenance windows are instead
          `asset__constraints.commitment`.
      unit_commitment:
        $ref: '#/$defs/ToolFeatureModel'
        default:
          source: []
          value: '?'
        description: Assets can be committed as units and _then_ economically dispatched.
          This generally requires assets to be modelled as discrete units which reflect
          assets 1-to-1.
    title: AssetOperatingRepresentationModel
    type: object
  DataIoBinaryFeatureModel:
    additionalProperties: false
    properties:
      source:
        description: 'Link(s) to source to validate the given value.

          This is usually used to validate a `y` (documentation link) or `dev` (issue
          or pull request link) but can feasibly be used to validate an `n`.'
        items:
          anyOf:
          - format: uri
            maxLength: 2083
            minLength: 1
            type: string
          - additionalProperties:
              format: uri
              maxLength: 2083
              minLength: 1
              type: string
            maxProperties: 1
            minProperties: 1
            propertyNames:
              enum:
              - input
              - output
            type: object
        title: Source
        type: array
      value:
        $ref: '#/$defs/DataIoBinaryValueModel'
        default:
          input: '?'
          output: '?'
        description: Per-slice feature value.
    title: DataIoBinaryFeatureModel
    type: object
  DataIoBinaryValueModel:
    additionalProperties: false
    properties:
      input:
        default: '?'
        description: Input data can be provided in this format.
        enum:
        - y
        - n
        - dev
        - '?'
        title: Input
        type: string
      output:
        default: '?'
        description: Output data is written in this format.
        enum:
        - y
        - n
        - dev
        - '?'
        title: Output
        type: string
    title: DataIoBinaryValueModel
    type: object
  DataIoDatabaseFeatureModel:
    additionalProperties: false
    properties:
      source:
        description: 'Link(s) to source to validate the given value.

          This is usually used to validate a `y` (documentation link) or `dev` (issue
          or pull request link) but can feasibly be used to validate an `n`.'
        items:
          anyOf:
          - format: uri
            maxLength: 2083
            minLength: 1
            type: string
          - additionalProperties:
              format: uri
              maxLength: 2083
              minLength: 1
              type: string
            maxProperties: 1
            minProperties: 1
            propertyNames:
              enum:
              - input
              - output
            type: object
        title: Source
        type: array
      value:
        $ref: '#/$defs/DataIoDatabaseValueModel'
        default:
          input: '?'
          output: '?'
        description: Per-slice feature value.
    title: DataIoDatabaseFeatureModel
    type: object
  DataIoDatabaseValueModel:
    additionalProperties: false
    properties:
      input:
        default: '?'
        description: Input data can be read from a database.
        enum:
        - y
        - n
        - dev
        - '?'
        title: Input
        type: string
      output:
        default: '?'
        description: Output data can be written to a database.
        enum:
        - y
        - n
        - dev
        - '?'
        title: Output
        type: string
    title: DataIoDatabaseValueModel
    type: object
  DataIoLazyLoadingFeatureModel:
    additionalProperties: false
    properties:
      source:
        description: 'Link(s) to source to validate the given value.

          This is usually used to validate a `y` (documentation link) or `dev` (issue
          or pull request link) but can feasibly be used to validate an `n`.'
        items:
          anyOf:
          - format: uri
            maxLength: 2083
            minLength: 1
            type: string
          - additionalProperties:
              format: uri
              maxLength: 2083
              minLength: 1
              type: string
            maxProperties: 1
            minProperties: 1
            propertyNames:
              enum:
              - input
              - output
            type: object
        title: Source
        type: array
      value:
        $ref: '#/$defs/DataIoLazyLoadingValueModel'
        default:
          input: '?'
          output: '?'
        description: Per-slice feature value.
    title: DataIoLazyLoadingFeatureModel
    type: object
  DataIoLazyLoadingValueModel:
    additionalProperties: false
    properties:
      input:
        default: '?'
        description: Input data can be loaded lazily.
        enum:
        - y
        - n
        - dev
        - '?'
        title: Input
        type: string
      output:
        default: '?'
        description: Output data can be written lazily / streamed rather than held
          fully in memory.
        enum:
        - y
        - n
        - dev
        - '?'
        title: Output
        type: string
    title: DataIoLazyLoadingValueModel
    type: object
  DataIoModel:
    additionalProperties: false
    properties:
      binary:
        $ref: '#/$defs/DataIoBinaryFeatureModel'
        default:
          source: []
          value:
            input: '?'
            output: '?'
        description: Data in compact / compressible, non-human-readable binary formats
          (e.g. Excel, NetCDF, HDF5, Parquet).
      database:
        $ref: '#/$defs/DataIoDatabaseFeatureModel'
        default:
          source: []
          value:
            input: '?'
            output: '?'
        description: Data in databases (relational or otherwise).
      lazy_loading:
        $ref: '#/$defs/DataIoLazyLoadingFeatureModel'
        default:
          source: []
          value:
            input: '?'
            output: '?'
        description: Data can be loaded lazily / out-of-core, allowing datasets larger
          than memory.
      plain_text:
        $ref: '#/$defs/DataIoPlainTextFeatureModel'
        default:
          source: []
          value:
            input: '?'
            output: '?'
        description: Data in human-readable, diff- and version-control-friendly text
          formats (e.g. CSV, JSON, YAML).
      remote_storage:
        $ref: '#/$defs/DataIoRemoteStorageFeatureModel'
        default:
          source: []
          value:
            input: '?'
            output: '?'
        description: Data can be read from / written to remote storage (e.g. object
          stores, cloud drives).
    title: DataIoModel
    type: object
  DataIoPlainTextFeatureModel:
    additionalProperties: false
    properties:
      source:
        description: 'Link(s) to source to validate the given value.

          This is usually used to validate a `y` (documentation link) or `dev` (issue
          or pull request link) but can feasibly be used to validate an `n`.'
        items:
          anyOf:
          - format: uri
            maxLength: 2083
            minLength: 1
            type: string
          - additionalProperties:
              format: uri
              maxLength: 2083
              minLength: 1
              type: string
            maxProperties: 1
            minProperties: 1
            propertyNames:
              enum:
              - input
              - output
            type: object
        title: Source
        type: array
      value:
        $ref: '#/$defs/DataIoPlainTextValueModel'
        default:
          input: '?'
          output: '?'
        description: Per-slice feature value.
    title: DataIoPlainTextFeatureModel
    type: object
  DataIoPlainTextValueModel:
    additionalProperties: false
    properties:
      input:
        default: '?'
        description: Input data can be provided in this format.
        enum:
        - y
        - n
        - dev
        - '?'
        title: Input
        type: string
      output:
        default: '?'
        description: Output data is written in this format.
        enum:
        - y
        - n
        - dev
        - '?'
        title: Output
        type: string
    title: DataIoPlainTextValueModel
    type: object
  DataIoRemoteStorageFeatureModel:
    additionalProperties: false
    properties:
      source:
        description: 'Link(s) to source to validate the given value.

          This is usually used to validate a `y` (documentation link) or `dev` (issue
          or pull request link) but can feasibly be used to validate an `n`.'
        items:
          anyOf:
          - format: uri
            maxLength: 2083
            minLength: 1
            type: string
          - additionalProperties:
              format: uri
              maxLength: 2083
              minLength: 1
              type: string
            maxProperties: 1
            minProperties: 1
            propertyNames:
              enum:
              - input
              - output
            type: object
        title: Source
        type: array
      value:
        $ref: '#/$defs/DataIoRemoteStorageValueModel'
        default:
          input: '?'
          output: '?'
        description: Per-slice feature value.
    title: DataIoRemoteStorageFeatureModel
    type: object
  DataIoRemoteStorageValueModel:
    additionalProperties: false
    properties:
      input:
        default: '?'
        description: Input data can be read from remote storage.
        enum:
        - y
        - n
        - dev
        - '?'
        title: Input
        type: string
      output:
        default: '?'
        description: Output data can be written to remote storage.
        enum:
        - y
        - n
        - dev
        - '?'
        title: Output
        type: string
    title: DataIoRemoteStorageValueModel
    type: object
  EdgeEffectsModel:
    additionalProperties: false
    properties:
      energy_balance:
        $ref: '#/$defs/ToolFeatureModel'
        default:
          source: []
          value: '?'
        description: Energy 'destruction' caused by simultaneous in- & out-flow from
          a lossy transmission/storage asset.
      time_financial:
        $ref: '#/$defs/ToolFeatureModel'
        default:
          source: []
          value: '?'
        description: Start- / End-of-horizon financial effects (e.g. asset investment
          annuitisation).
      time_operational:
        $ref: '#/$defs/ToolFeatureModel'
        default:
          source: []
          value: '?'
        description: Start- / End-of-horizon operational effects (e.g. storage level
          wrapping).
    title: EdgeEffectsModel
    type: object
  FeatureSetModel:
    properties:
      actors:
        $ref: '#/$defs/ActorsModel'
        default:
          distributed_objectives:
            source: []
            value: '?'
          equilibrium:
            source: []
            value: '?'
          portfolio:
            source: []
            value: '?'
        description: Representation of heterogeneous decision-making actors, deviating
          from the single, central-planner baseline.
      asset__capacity_representation:
        $ref: '#/$defs/AssetCapacityRepresentationModel'
        default:
          continuous_capacity:
            source: []
            value: '?'
          exogenous_capacity:
            source: []
            value: '?'
          integer_investment_continuous_capacity:
            source: []
            value: '?'
          integer_investment_exogenous_capacity:
            source: []
            value: '?'
        description: How asset capacities can be represented in the model and optimisation
          problem.
      asset__characteristics:
        $ref: '#/$defs/AssetCharacteristicsModel'
        default:
          configurable_dimensions:
            source: []
            value:
              investment: '?'
              operation: '?'
          extensible_dimensions:
            source: []
            value:
              investment: '?'
              operation: '?'
          linear:
            source: []
            value:
              investment: '?'
              operation: '?'
          nonlinear:
            source: []
            value:
              investment: '?'
              operation: '?'
          path_dependent:
            source: []
            value:
              investment: '?'
              operation: '?'
          quadratic:
            source: []
            value:
              investment: '?'
              operation: '?'
        description: Representation of asset investment and operating characteristics
          (e.g. efficiency, resource availability).
      asset__constraints:
        $ref: '#/$defs/AssetConstraintsModel'
        default:
          boundary_conditions:
            source: []
            value:
              investment: '?'
              operation: '?'
          commitment:
            source: []
            value:
              investment: '?'
              operation: '?'
          dedicated_flow_routing:
            source: []
            value: '?'
          inter_asset:
            source: []
            value:
              investment: '?'
              operation: '?'
          inter_spatial:
            source: []
            value:
              investment: '?'
              operation: '?'
          inter_temporal:
            source: []
            value:
              investment: '?'
              operation: '?'
          intra_temporal:
            source: []
            value:
              investment: '?'
              operation: '?'
          total:
            source: []
            value:
              investment: '?'
              operation: '?'
        description: Upper/lower limits on asset investment and operating decisions.
      asset__cost:
        $ref: '#/$defs/AssetCostModel'
        default:
          annuitisation:
            source: []
            value: '?'
          inter_temporal:
            source: []
            value:
              investment: '?'
              operation: '?'
          linear:
            source: []
            value:
              investment: '?'
              operation: '?'
          nonlinear:
            source: []
            value:
              investment: '?'
              operation: '?'
          threshold_deviation:
            source: []
            value:
              investment: '?'
              operation: '?'
          unit:
            source: []
            value:
              investment: '?'
              operation: '?'
        description: Asset investment and operating cost representation.
      asset__operating_representation:
        $ref: '#/$defs/AssetOperatingRepresentationModel'
        default:
          maintenance_scheduling:
            source: []
            value: '?'
          unit_commitment:
            source: []
            value: '?'
        description: The resolution of asset operation representation in the model.
          A linearised approximation of unit commitment is `tractability__reformulation.linearised_unit_commitment`.
      data__io:
        $ref: '#/$defs/DataIoModel'
        default:
          binary:
            source: []
            value:
              input: '?'
              output: '?'
          database:
            source: []
            value:
              input: '?'
              output: '?'
          lazy_loading:
            source: []
            value:
              input: '?'
              output: '?'
          plain_text:
            source: []
            value:
              input: '?'
              output: '?'
          remote_storage:
            source: []
            value:
              input: '?'
              output: '?'
        description: Data formats and mechanisms for moving model input/output data.
          Computing new parameters is `preprocessing`.
      edge_effects:
        $ref: '#/$defs/EdgeEffectsModel'
        default:
          energy_balance:
            source: []
            value: '?'
          time_financial:
            source: []
            value: '?'
          time_operational:
            source: []
            value: '?'
        description: Distortive effects of system boundaries and linearising model
          components can be mitigated against. Flows across the spatial boundary are
          `system__processes.boundary_exchange`.
      interface:
        $ref: '#/$defs/InterfaceModel'
        default:
          analysis_templates:
            source: []
            value: '?'
          api:
            source: []
            value:
              analyse: '?'
              build: '?'
              run: '?'
          cli:
            source: []
            value:
              analyse: '?'
              build: '?'
              run: '?'
          component_templating:
            source: []
            value: '?'
          dimension_groups:
            source: []
            value: '?'
          gui:
            source: []
            value:
              analyse: '?'
              build: '?'
              run: '?'
          remote_execution:
            source: []
            value: '?'
          solver_configuration:
            source: []
            value: '?'
          standardised_data:
            source: []
            value:
              analyse: '?'
              build: '?'
        description: 'Available options across the model workflow: defining, configuring,
          and structuring a model (`build`); executing a model run (`run`); and interacting
          with model outputs (`analyse`).'
      interface__math:
        $ref: '#/$defs/InterfaceMathModel'
        default:
          dual_extraction:
            source: []
            value: '?'
          formulation_transparency:
            source: []
            value: '?'
          piecewise_formulation:
            source: []
            value: '?'
          problem_file_export:
            source: []
            value: '?'
          soft_constraints:
            source: []
            value: '?'
          user_defined_math:
            source: []
            value: '?'
        description: Access to, and user extension of, the underlying mathematical
          formulation.
      network__electricity:
        $ref: '#/$defs/NetworkElectricityModel'
        default:
          network_devices:
            source: []
            value:
              facts_devices: '?'
              hvdc_converters: '?'
              transformers: '?'
          network_limits:
            source: []
            value:
              phase_angle: '?'
              thermal: '?'
          optimal_power_flow:
            source: []
            value:
              ac_opf: '?'
              dc_opf: '?'
          sensitivity_limits:
            source: []
            value:
              generic_constraints: '?'
              outage_factors: '?'
              ptdf: '?'
          unbalanced_flows:
            source: []
            value: '?'
        description: Electrical network representation beyond the `system__processes.spatial_transfer`
          baseline.
      network__pipeline:
        $ref: '#/$defs/NetworkPipelineModel'
        default:
          composition_tracking:
            source: []
            value: '?'
          linearised_flow:
            source: []
            value: '?'
        description: Pressure-driven network representation for carriers transported
          through pipes, beyond the `system__processes.spatial_transfer` baseline
          (e.g. methane, hydrogen, CO2, district heat/cooling, water).
      optimisation__objective:
        $ref: '#/$defs/OptimisationObjectiveModel'
        default:
          multi_objective_pareto:
            source: []
            value: '?'
          multi_objective_weighted:
            source: []
            value: '?'
          quadratic:
            source: []
            value: '?'
        description: Objective-function options beyond minimisation of a single linear
          cost term.
      orchestration:
        $ref: '#/$defs/OrchestrationModel'
        default:
          rolling_horizon:
            source: []
            value: '?'
          sampled_runs:
            source: []
            value: '?'
          scenario_runs:
            source: []
            value: '?'
          simulation_optimisation:
            source: []
            value: '?'
        description: Tool-managed workflows coordinating multiple complete runs in
          series/parallel, where the result emerges from the ensemble or its convergence.
      postprocessing:
        $ref: '#/$defs/PostprocessingModel'
        default:
          aggregation:
            source: []
            value:
              assets: '?'
              spatial: '?'
              temporal: '?'
          disaggregation:
            source: []
            value:
              assets: '?'
              spatial: '?'
              temporal: '?'
          financial:
            source: []
            value:
              asset_accounts: '?'
              contract_settlement: '?'
              levelised_cost: '?'
              marginal_price: '?'
              net_present_value: '?'
              water_value: '?'
          impact_assessment:
            source: []
            value:
              emissions: '?'
              resource_use: '?'
              socioeconomic: '?'
          network_transfer_metrics:
            source: []
            value:
              flow_sensitivity: '?'
              transfer_capacity: '?'
          operational:
            source: []
            value:
              capacity_factor: '?'
              curtailment: '?'
              full_load_hours: '?'
          probabilistic_reliability_assessment:
            source: []
            value:
              capacity_credit: '?'
              loss_of_load: '?'
              unserved_energy: '?'
          scenario_comparison:
            source: []
            value: '?'
        description: Postprocessing analyses that can be triggered by the tool based
          on model outputs, on a per-asset or system-wide basis.
      preprocessing:
        $ref: '#/$defs/PreprocessingModel'
        default:
          download:
            source: []
            value: '?'
          forecasting:
            source: []
            value: '?'
          input_validation:
            source: []
            value: '?'
          model_catalogue:
            source: []
            value: '?'
          numeric_scaling:
            source: []
            value: '?'
          scenario_generation:
            source: []
            value: '?'
          spatial_distribution:
            source: []
            value:
              demand: '?'
              network_geometry: '?'
              slack: '?'
        description: Options to generate parameters derived from user-defined input
          data before running the model. Moving/storing data is `data__io`.
      system__processes:
        $ref: '#/$defs/SystemProcessesModel'
        default:
          boundary_exchange:
            source: []
            value: '?'
          buffered_transfer:
            source: []
            value: '?'
          conversion:
            source: []
            value: '?'
          delayed_transfer:
            source: []
            value: '?'
          discrete_transport:
            source: []
            value: '?'
          explicit_demand:
            source: []
            value: '?'
          spatial_transfer:
            source: []
            value: '?'
          state_changing_transfer:
            source: []
            value: '?'
          temporal_transfer:
            source: []
            value: '?'
        description: Fundamental carrier-flow capabilities the tool can represent,
          defined by their mathematical role rather than by named technologies. `Carriers`
          are the energy or non-energy commodities that flow between processes (e.g.
          electricity, heat, hydrogen, CO2, water, materials). Only physical processes
          are in scope; decision-making agents are `actors`.
      system__reliability:
        $ref: '#/$defs/SystemReliabilityModel'
        default:
          contingency_constrained:
            source: []
            value: '?'
          margin_requirement:
            source: []
            value:
              capacity: '?'
              operating_reserve: '?'
          probabilistic_reliability_limit:
            source: []
            value:
              loss_of_load: '?'
              unserved_energy: '?'
          unserved_demand:
            source: []
            value: '?'
        description: Reliability requirements imposed as constraints or penalty costs
          within the optimisation, under deterministic inputs. Post-hoc adequacy assessment
          is `postprocessing.probabilistic_reliability_assessment`; probabilistic
          decision methods are `uncertainty`.
      time__pathway:
        $ref: '#/$defs/TimePathwayModel'
        default:
          myopic_foresight:
            source: []
            value:
              investment: '?'
              operation: '?'
          perfect_foresight:
            source: []
            value:
              investment: '?'
              operation: '?'
          singular:
            source: []
            value:
              investment: '?'
              operation: '?'
        description: Foresight extent and planning direction across operational (intra-annual)
          or investment (inter-annual) time periods.
      tractability__dimension_reduction:
        $ref: '#/$defs/TractabilityDimensionReductionModel'
        default:
          assets:
            source: []
            value:
              black_boxing: '?'
              clustering: '?'
              component_removal: '?'
          spatial:
            source: []
            value:
              clustering: '?'
              network_reduction: '?'
          temporal:
            source: []
            value:
              dimension_non_contiguity: '?'
              load_duration_curves: '?'
              resampling: '?'
              typical_periods: '?'
        description: Options to reduce the size of a given dimension of the problem.
      tractability__reformulation:
        $ref: '#/$defs/TractabilityReformulationModel'
        default:
          approximation:
            source: []
            value: '?'
          constraint_screening:
            source: []
            value: '?'
          convex_relaxation:
            source: []
            value: '?'
          decomposition:
            source: []
            value: '?'
          function_conversion:
            source: []
            value: '?'
          linearised_unit_commitment:
            source: []
            value: '?'
          variable_fidelity:
            source: []
            value: '?'
        description: Approximations of discrete or non-linear formulations that preserve
          (mixed-integer) linear tractability.
      uncertainty:
        $ref: '#/$defs/UncertaintyModel'
        default:
          multi_stage_stochastic:
            source: []
            value: '?'
          near_optimal:
            source: []
            value: '?'
          risk_aversion:
            source: []
            value: '?'
          robust_optimisation:
            source: []
            value: '?'
          two_stage_stochastic:
            source: []
            value: '?'
        description: Methods that restructure the decision problem to account for
          parametric or structural uncertainty.
      workflow__transparency:
        $ref: '#/$defs/WorkflowTransparencyModel'
        default:
          infeasibility_diagnosis:
            source: []
            value: '?'
          intermediate_access:
            source: []
            value: '?'
          metadata_propagation:
            source: []
            value:
              provenance: '?'
              units: '?'
          run_logs:
            source: []
            value: '?'
        description: 'Transparency of the modelling pipeline: logs, diagnostics, and
          traceability of data as it moves through the workflow.'
    title: FeatureSetModel
    type: object
  InterfaceApiFeatureModel:
    additionalProperties: false
    properties:
      source:
        description: 'Link(s) to source to validate the given value.

          This is usually used to validate a `y` (documentation link) or `dev` (issue
          or pull request link) but can feasibly be used to validate an `n`.'
        items:
          anyOf:
          - format: uri
            maxLength: 2083
            minLength: 1
            type: string
          - additionalProperties:
              format: uri
              maxLength: 2083
              minLength: 1
              type: string
            maxProperties: 1
            minProperties: 1
            propertyNames:
              enum:
              - build
              - run
              - analyse
            type: object
        title: Source
        type: array
      value:
        $ref: '#/$defs/InterfaceApiValueModel'
        default:
          analyse: '?'
          build: '?'
          run: '?'
        description: Per-slice feature value.
    title: InterfaceApiFeatureModel
    type: object
  InterfaceApiValueModel:
    additionalProperties: false
    properties:
      analyse:
        default: '?'
        description: Programmatic interface to analyse results.
        enum:
        - y
        - n
        - dev
        - '?'
        title: Analyse
        type: string
      build:
        default: '?'
        description: Can be configured using scripting specific to the tool programming
          language or another programming language for which an API is provided (e.g.
          a Python API for a package written in C).
        enum:
        - y
        - n
        - dev
        - '?'
        title: Build
        type: string
      run:
        default: '?'
        description: Programmatic interface to launch and manage runs.
        enum:
        - y
        - n
        - dev
        - '?'
        title: Run
        type: string
    title: InterfaceApiValueModel
    type: object
  InterfaceCliFeatureModel:
    additionalProperties: false
    properties:
      source:
        description: 'Link(s) to source to validate the given value.

          This is usually used to validate a `y` (documentation link) or `dev` (issue
          or pull request link) but can feasibly be used to validate an `n`.'
        items:
          anyOf:
          - format: uri
            maxLength: 2083
            minLength: 1
            type: string
          - additionalProperties:
              format: uri
              maxLength: 2083
              minLength: 1
              type: string
            maxProperties: 1
            minProperties: 1
            propertyNames:
              enum:
              - build
              - run
              - analyse
            type: object
        title: Source
        type: array
      value:
        $ref: '#/$defs/InterfaceCliValueModel'
        default:
          analyse: '?'
          build: '?'
          run: '?'
        description: Per-slice feature value.
    title: InterfaceCliFeatureModel
    type: object
  InterfaceCliValueModel:
    additionalProperties: false
    properties:
      analyse:
        default: '?'
        description: CLI commands to create reports to enable result analysis.
        enum:
        - y
        - n
        - dev
        - '?'
        title: Analyse
        type: string
      build:
        default: '?'
        description: CLI commands that enable model building. This is usually used
          to convert between data formats for a given model definition, but may also
          be used to redefine a model (e.g. reduce dimensions).
        enum:
        - y
        - n
        - dev
        - '?'
        title: Build
        type: string
      run:
        default: '?'
        description: CLI commands that launch and manage a model run.
        enum:
        - y
        - n
        - dev
        - '?'
        title: Run
        type: string
    title: InterfaceCliValueModel
    type: object
  InterfaceGuiFeatureModel:
    additionalProperties: false
    properties:
      source:
        description: 'Link(s) to source to validate the given value.

          This is usually used to validate a `y` (documentation link) or `dev` (issue
          or pull request link) but can feasibly be used to validate an `n`.'
        items:
          anyOf:
          - format: uri
            maxLength: 2083
            minLength: 1
            type: string
          - additionalProperties:
              format: uri
              maxLength: 2083
              minLength: 1
              type: string
            maxProperties: 1
            minProperties: 1
            propertyNames:
              enum:
              - build
              - run
              - analyse
            type: object
        title: Source
        type: array
      value:
        $ref: '#/$defs/InterfaceGuiValueModel'
        default:
          analyse: '?'
          build: '?'
          run: '?'
        description: Per-slice feature value.
    title: InterfaceGuiFeatureModel
    type: object
  InterfaceGuiValueModel:
    additionalProperties: false
    properties:
      analyse:
        default: '?'
        description: A GUI is available for interacting with model outputs.
        enum:
        - y
        - n
        - dev
        - '?'
        title: Analyse
        type: string
      build:
        default: '?'
        description: A GUI is available for defining, configuring, and structuring
          a model.
        enum:
        - y
        - n
        - dev
        - '?'
        title: Build
        type: string
      run:
        default: '?'
        description: A GUI is available for executing a model run.
        enum:
        - y
        - n
        - dev
        - '?'
        title: Run
        type: string
    title: InterfaceGuiValueModel
    type: object
  InterfaceMathModel:
    additionalProperties: false
    properties:
      dual_extraction:
        $ref: '#/$defs/ToolFeatureModel'
        default:
          source: []
          value: '?'
        description: The built optimisation problem's constraint duals can be extracted
          (e.g. to approximate wholesale market prices via `postprocessing.financial[marginal_price]`,
          or to construct a future cost function via `postprocessing.financial[water_value]`).
      formulation_transparency:
        $ref: '#/$defs/ToolFeatureModel'
        default:
          source: []
          value: '?'
        description: The tool's mathematical formulation is accessible and inspectable.
          (e.g. as rendered equations or a math specification traceable to the built
          problem).
      piecewise_formulation:
        $ref: '#/$defs/ToolFeatureModel'
        default:
          source: []
          value: '?'
        description: Piecewise formulations of nonlinear relationships can be defined
          in the math (e.g. using LP chord-line or MIP SOS2 constraints).
      problem_file_export:
        $ref: '#/$defs/ToolFeatureModel'
        default:
          source: []
          value: '?'
        description: The built optimisation problem can be exported in standard formats
          (e.g. LP/MPS).
      soft_constraints:
        $ref: '#/$defs/ToolFeatureModel'
        default:
          source: []
          value: '?'
        description: User-defined or built-in constraints can be reformulated as soft
          constraints without bespoke implementation per constraint. This is usually
          via a generic slack-variable-plus-penalty mechanism. Built-in economic threshold
          costs are instead `asset__cost.threshold_deviation[investment]` / `asset__cost.threshold_deviation[operation]`.
      user_defined_math:
        $ref: '#/$defs/ToolFeatureModel'
        default:
          source: []
          value: '?'
        description: Users can define their own constraints on the optimisation problem,
          applied to existing or new decision variables, and write their own objective
          expression.
    title: InterfaceMathModel
    type: object
  InterfaceModel:
    additionalProperties: false
    properties:
      analysis_templates:
        $ref: '#/$defs/ToolFeatureModel'
        default:
          source: []
          value: '?'
        description: Templates can be used to generate consistent reports or dashboards
          to consolidate model outputs for dissemination.
      api:
        $ref: '#/$defs/InterfaceApiFeatureModel'
        default:
          source: []
          value:
            analyse: '?'
            build: '?'
            run: '?'
        description: Programmatic interface (e.g. language API, web API).
      cli:
        $ref: '#/$defs/InterfaceCliFeatureModel'
        default:
          source: []
          value:
            analyse: '?'
            build: '?'
            run: '?'
        description: Command line interface.
      component_templating:
        $ref: '#/$defs/ToolFeatureModel'
        default:
          source: []
          value: '?'
        description: Components can inherit parameterisation from a shared template
          (e.g. an abstract technology definition), with edits to the template cascading
          to every inheriting component, rather than requiring per-component data
          entry.
      dimension_groups:
        $ref: '#/$defs/ToolFeatureModel'
        default:
          source: []
          value: '?'
        description: Dimension members can be tagged into named, potentially overlapping
          groups independent of the model's built-in structural dimensions (e.g. a
          "coastal assets" or "2030-vintage" tag spanning multiple asset types/nodes).
          These can be used for dimension aggregation to achieve tractability (e.g.
          spatial nodes to zones), for filtering on dimensions when applying constraints
          (e.g. total capacity across group X; seasonal limits), and for analysis/visualisation
          (`postprocessing.aggregation`/`postprocessing.disaggregation`).
      gui:
        $ref: '#/$defs/InterfaceGuiFeatureModel'
        default:
          source: []
          value:
            analyse: '?'
            build: '?'
            run: '?'
        description: Graphical user interface.
      remote_execution:
        $ref: '#/$defs/ToolFeatureModel'
        default:
          source: []
          value: '?'
        description: Runs can be dispatched to and managed on remote compute (cloud,
          HPC, hosted service) from the tool.
      solver_configuration:
        $ref: '#/$defs/ToolFeatureModel'
        default:
          source: []
          value: '?'
        description: Optimiser parameters can be directly configured, including convergence
          parameters & warm-starting options.
      standardised_data:
        $ref: '#/$defs/InterfaceStandardisedDataFeatureModel'
        default:
          source: []
          value:
            analyse: '?'
            build: '?'
        description: Accepts or emits data following a community-recognised convention
          or ontology (e.g. Open Energy Ontology <OEO>, IAMC template, the Energy
          System Description Language <ESDL>, Common Energy System Model <CESM>, SiennaSchemas).
    title: InterfaceModel
    type: object
  InterfaceStandardisedDataFeatureModel:
    additionalProperties: false
    properties:
      source:
        description: 'Link(s) to source to validate the given value.

          This is usually used to validate a `y` (documentation link) or `dev` (issue
          or pull request link) but can feasibly be used to validate an `n`.'
        items:
          anyOf:
          - format: uri
            maxLength: 2083
            minLength: 1
            type: string
          - additionalProperties:
              format: uri
              maxLength: 2083
              minLength: 1
              type: string
            maxProperties: 1
            minProperties: 1
            propertyNames:
              enum:
              - build
              - analyse
            type: object
        title: Source
        type: array
      value:
        $ref: '#/$defs/InterfaceStandardisedDataValueModel'
        default:
          analyse: '?'
          build: '?'
        description: Per-slice feature value.
    title: InterfaceStandardisedDataFeatureModel
    type: object
  InterfaceStandardisedDataValueModel:
    additionalProperties: false
    properties:
      analyse:
        default: '?'
        description: Outputs follow a community-recognised convention or ontology.
          This is useful for regulatory reporting or frictionless data transfer with
          a downstream process.
        enum:
        - y
        - n
        - dev
        - '?'
        title: Analyse
        type: string
      build:
        default: '?'
        description: Accepts input data following a community-recognised convention
          or ontology.
        enum:
        - y
        - n
        - dev
        - '?'
        title: Build
        type: string
    title: InterfaceStandardisedDataValueModel
    type: object
  NetworkElectricityModel:
    additionalProperties: false
    properties:
      network_devices:
        $ref: '#/$defs/NetworkElectricityNetworkDevicesFeatureModel'
        default:
          source: []
          value:
            facts_devices: '?'
            hvdc_converters: '?'
            transformers: '?'
        description: Network components beyond simple lines and buses, carrying their
          own mathematical formulations.
      network_limits:
        $ref: '#/$defs/NetworkElectricityNetworkLimitsFeatureModel'
        default:
          source: []
          value:
            phase_angle: '?'
            thermal: '?'
        description: Network operating limits representable in the power-flow formulation.
      optimal_power_flow:
        $ref: '#/$defs/NetworkElectricityOptimalPowerFlowFeatureModel'
        default:
          source: []
          value:
            ac_opf: '?'
            dc_opf: '?'
        description: Linearised optimal power flow can be modelled.
      sensitivity_limits:
        $ref: '#/$defs/NetworkElectricitySensitivityLimitsFeatureModel'
        default:
          source: []
          value:
            generic_constraints: '?'
            outage_factors: '?'
            ptdf: '?'
        description: Flows can be limited using supplied linear sensitivities, as
          an alternative to physical network limits.
      unbalanced_flows:
        $ref: '#/$defs/ToolFeatureModel'
        default:
          source: []
          value: '?'
        description: Unbalanced flows (asymmetric phases) can be handled in optimal
          power flow, usually using linearised DistFlow branch equations.
    title: NetworkElectricityModel
    type: object
  NetworkElectricityNetworkDevicesFeatureModel:
    additionalProperties: false
    properties:
      source:
        description: 'Link(s) to source to validate the given value.

          This is usually used to validate a `y` (documentation link) or `dev` (issue
          or pull request link) but can feasibly be used to validate an `n`.'
        items:
          anyOf:
          - format: uri
            maxLength: 2083
            minLength: 1
            type: string
          - additionalProperties:
              format: uri
              maxLength: 2083
              minLength: 1
              type: string
            maxProperties: 1
            minProperties: 1
            propertyNames:
              enum:
              - transformers
              - facts_devices
              - hvdc_converters
            type: object
        title: Source
        type: array
      value:
        $ref: '#/$defs/NetworkElectricityNetworkDevicesValueModel'
        default:
          facts_devices: '?'
          hvdc_converters: '?'
          transformers: '?'
        description: Per-slice feature value.
    title: NetworkElectricityNetworkDevicesFeatureModel
    type: object
  NetworkElectricityNetworkDevicesValueModel:
    additionalProperties: false
    properties:
      facts_devices:
        default: '?'
        description: Flexible AC Transmission System <FACTS> devices.
        enum:
        - y
        - n
        - dev
        - '?'
        title: Facts Devices
        type: string
      hvdc_converters:
        default: '?'
        description: Rectifiers, inverters, and HVDC converter stations.
        enum:
        - y
        - n
        - dev
        - '?'
        title: Hvdc Converters
        type: string
      transformers:
        default: '?'
        description: Shunt and phase-shifting transformers.
        enum:
        - y
        - n
        - dev
        - '?'
        title: Transformers
        type: string
    title: NetworkElectricityNetworkDevicesValueModel
    type: object
  NetworkElectricityNetworkLimitsFeatureModel:
    additionalProperties: false
    properties:
      source:
        description: 'Link(s) to source to validate the given value.

          This is usually used to validate a `y` (documentation link) or `dev` (issue
          or pull request link) but can feasibly be used to validate an `n`.'
        items:
          anyOf:
          - format: uri
            maxLength: 2083
            minLength: 1
            type: string
          - additionalProperties:
              format: uri
              maxLength: 2083
              minLength: 1
              type: string
            maxProperties: 1
            minProperties: 1
            propertyNames:
              enum:
              - thermal
              - phase_angle
            type: object
        title: Source
        type: array
      value:
        $ref: '#/$defs/NetworkElectricityNetworkLimitsValueModel'
        default:
          phase_angle: '?'
          thermal: '?'
        description: Per-slice feature value.
    title: NetworkElectricityNetworkLimitsFeatureModel
    type: object
  NetworkElectricityNetworkLimitsValueModel:
    additionalProperties: false
    properties:
      phase_angle:
        default: '?'
        description: Phase-angle difference limits between connected buses.
        enum:
        - y
        - n
        - dev
        - '?'
        title: Phase Angle
        type: string
      thermal:
        default: '?'
        description: Thermal (branch flow magnitude) limits.
        enum:
        - y
        - n
        - dev
        - '?'
        title: Thermal
        type: string
    title: NetworkElectricityNetworkLimitsValueModel
    type: object
  NetworkElectricityOptimalPowerFlowFeatureModel:
    additionalProperties: false
    properties:
      source:
        description: 'Link(s) to source to validate the given value.

          This is usually used to validate a `y` (documentation link) or `dev` (issue
          or pull request link) but can feasibly be used to validate an `n`.'
        items:
          anyOf:
          - format: uri
            maxLength: 2083
            minLength: 1
            type: string
          - additionalProperties:
              format: uri
              maxLength: 2083
              minLength: 1
              type: string
            maxProperties: 1
            minProperties: 1
            propertyNames:
              enum:
              - dc_opf
              - ac_opf
            type: object
        title: Source
        type: array
      value:
        $ref: '#/$defs/NetworkElectricityOptimalPowerFlowValueModel'
        default:
          ac_opf: '?'
          dc_opf: '?'
        description: Per-slice feature value.
    title: NetworkElectricityOptimalPowerFlowFeatureModel
    type: object
  NetworkElectricityOptimalPowerFlowValueModel:
    additionalProperties: false
    properties:
      ac_opf:
        default: '?'
        description: Linearised AC optimal power flow
        enum:
        - y
        - n
        - dev
        - '?'
        title: Ac Opf
        type: string
      dc_opf:
        default: '?'
        description: DC optimal power flow
        enum:
        - y
        - n
        - dev
        - '?'
        title: Dc Opf
        type: string
    title: NetworkElectricityOptimalPowerFlowValueModel
    type: object
  NetworkElectricitySensitivityLimitsFeatureModel:
    additionalProperties: false
    properties:
      source:
        description: 'Link(s) to source to validate the given value.

          This is usually used to validate a `y` (documentation link) or `dev` (issue
          or pull request link) but can feasibly be used to validate an `n`.'
        items:
          anyOf:
          - format: uri
            maxLength: 2083
            minLength: 1
            type: string
          - additionalProperties:
              format: uri
              maxLength: 2083
              minLength: 1
              type: string
            maxProperties: 1
            minProperties: 1
            propertyNames:
              enum:
              - ptdf
              - outage_factors
              - generic_constraints
            type: object
        title: Source
        type: array
      value:
        $ref: '#/$defs/NetworkElectricitySensitivityLimitsValueModel'
        default:
          generic_constraints: '?'
          outage_factors: '?'
          ptdf: '?'
        description: Per-slice feature value.
    title: NetworkElectricitySensitivityLimitsFeatureModel
    type: object
  NetworkElectricitySensitivityLimitsValueModel:
    additionalProperties: false
    properties:
      generic_constraints:
        default: '?'
        description: Generic transmission Constraints defined by user-supplied sensitivity
          coefficients not tied to a named physical factor.
        enum:
        - y
        - n
        - dev
        - '?'
        title: Generic Constraints
        type: string
      outage_factors:
        default: '?'
        description: Line/Branch Outage Distribution Factor <LODF/BODF> limits, describing
          the sensitivity of a branch flow to the outage of another branch.
        enum:
        - y
        - n
        - dev
        - '?'
        title: Outage Factors
        type: string
      ptdf:
        default: '?'
        description: Power Transfer Distribution Factor <PTDF> limits, describing
          the sensitivity of a branch flow to a nodal injection.
        enum:
        - y
        - n
        - dev
        - '?'
        title: Ptdf
        type: string
    title: NetworkElectricitySensitivityLimitsValueModel
    type: object
  NetworkPipelineModel:
    additionalProperties: false
    properties:
      composition_tracking:
        $ref: '#/$defs/ToolFeatureModel'
        default:
          source: []
          value: '?'
        description: A single transfer asset carries a mixture whose constituents
          are balanced individually, allowing limits on the resulting blend (e.g.
          hydrogen blend-fraction limits, minimum gross calorific value <GCV>, Wobbe
          index bounds). Composition-dependent hydraulics (e.g. a density-dependent
          pressure-drop coefficient) are out of scope.
      linearised_flow:
        $ref: '#/$defs/ToolFeatureModel'
        default:
          source: []
          value: '?'
        description: Nodal pressure/head is a decision variable which couples flows
          using a linearised pressure-drop relationship. Operating limits on that
          state are representable in the same formulation (e.g. minimum/maximum nodal
          pressure, maximum allowable operating pressure <MAOP>, contracted delivery
          pressure).
    title: NetworkPipelineModel
    type: object
  OptimisationObjectiveModel:
    additionalProperties: false
    properties:
      multi_objective_pareto:
        $ref: '#/$defs/ToolFeatureModel'
        default:
          source: []
          value: '?'
        description: Built-in generation of Pareto-optimal solution sets across objectives
          (e.g. epsilon-constraint, evolutionary methods).
      multi_objective_weighted:
        $ref: '#/$defs/ToolFeatureModel'
        default:
          source: []
          value: '?'
        description: Multiple objective terms combined via user-set weights into a
          single objectives. Trade-offs explored by manual weight variation.
      quadratic:
        $ref: '#/$defs/ToolFeatureModel'
        default:
          source: []
          value: '?'
        description: Quadratic objective terms passed natively to the solver (e.g.
          quadratic generation cost curves, welfare maximisation with linear demand
          curves).
    title: OptimisationObjectiveModel
    type: object
  OrchestrationModel:
    additionalProperties: false
    properties:
      rolling_horizon:
        $ref: '#/$defs/ToolFeatureModel'
        default:
          source: []
          value: '?'
        description: Myopic foresight pathway optimisation windows are optimised sequentially,
          with automatic handling of data pass-through between runs.
      sampled_runs:
        $ref: '#/$defs/ToolFeatureModel'
        default:
          source: []
          value: '?'
        description: Execution of run ensembles over a sampled uncertainty space (e.g.
          Monte Carlo), with collated outputs. Used e.g. for post-hoc resource adequacy
          assessment (`postprocessing.probabilistic_reliability_assessment`) or portfolio
          price-risk quantification. Distinct from `scenario_runs` (discrete, named
          scenarios).
      scenario_runs:
        $ref: '#/$defs/ToolFeatureModel'
        default:
          source: []
          value: '?'
        description: Batch execution of discrete, named scenario ensembles in series/parallel
          with collated outputs.
      simulation_optimisation:
        $ref: '#/$defs/ToolFeatureModel'
        default:
          source: []
          value: '?'
        description: Iterate between a simulation and optimisation phase until convergence
          with respect to (often nonlinear) constraints (e.g. competitive market simulation,
          optimal power flow).
    title: OrchestrationModel
    type: object
  PostprocessingAggregationFeatureModel:
    additionalProperties: false
    properties:
      source:
        description: 'Link(s) to source to validate the given value.

          This is usually used to validate a `y` (documentation link) or `dev` (issue
          or pull request link) but can feasibly be used to validate an `n`.'
        items:
          anyOf:
          - format: uri
            maxLength: 2083
            minLength: 1
            type: string
          - additionalProperties:
              format: uri
              maxLength: 2083
              minLength: 1
              type: string
            maxProperties: 1
            minProperties: 1
            propertyNames:
              enum:
              - temporal
              - spatial
              - assets
            type: object
        title: Source
        type: array
      value:
        $ref: '#/$defs/PostprocessingAggregationValueModel'
        default:
          assets: '?'
          spatial: '?'
          temporal: '?'
        description: Per-slice feature value.
    title: PostprocessingAggregationFeatureModel
    type: object
  PostprocessingAggregationValueModel:
    additionalProperties: false
    properties:
      assets:
        default: '?'
        description: Aggregation across assets that are members of user-defined groups
          (e.g., separately costed "wind turbine" assets aggregated back to "wind
          turbine").
        enum:
        - y
        - n
        - dev
        - '?'
        title: Assets
        type: string
      spatial:
        default: '?'
        description: Aggregation across spatial regions that are members of user-defined
          groups (e.g. results per country in a nodal analysis).
        enum:
        - y
        - n
        - dev
        - '?'
        title: Spatial
        type: string
      temporal:
        default: '?'
        description: Aggregation across time using standard datetime operations (e.g.
          seasonal, annual, decadal summations).
        enum:
        - y
        - n
        - dev
        - '?'
        title: Temporal
        type: string
    title: PostprocessingAggregationValueModel
    type: object
  PostprocessingDisaggregationFeatureModel:
    additionalProperties: false
    properties:
      source:
        description: 'Link(s) to source to validate the given value.

          This is usually used to validate a `y` (documentation link) or `dev` (issue
          or pull request link) but can feasibly be used to validate an `n`.'
        items:
          anyOf:
          - format: uri
            maxLength: 2083
            minLength: 1
            type: string
          - additionalProperties:
              format: uri
              maxLength: 2083
              minLength: 1
              type: string
            maxProperties: 1
            minProperties: 1
            propertyNames:
              enum:
              - temporal
              - spatial
              - assets
            type: object
        title: Source
        type: array
      value:
        $ref: '#/$defs/PostprocessingDisaggregationValueModel'
        default:
          assets: '?'
          spatial: '?'
          temporal: '?'
        description: Per-slice feature value.
    title: PostprocessingDisaggregationFeatureModel
    type: object
  PostprocessingDisaggregationValueModel:
    additionalProperties: false
    properties:
      assets:
        default: '?'
        description: Disaggregation across assets, usually to return output to align
          with the dimension prior to applying asset aggregation (`tractability__dimension_reduction`).
        enum:
        - y
        - n
        - dev
        - '?'
        title: Assets
        type: string
      spatial:
        default: '?'
        description: Disaggregation across space, usually to return output to align
          with the dimension prior to applying spatial aggregation (`tractability__dimension_reduction`).
        enum:
        - y
        - n
        - dev
        - '?'
        title: Spatial
        type: string
      temporal:
        default: '?'
        description: Disaggregation across time, usually to align output data with
          the dimension prior to applying temporal aggregation (`tractability__dimension_reduction`).
        enum:
        - y
        - n
        - dev
        - '?'
        title: Temporal
        type: string
    title: PostprocessingDisaggregationValueModel
    type: object
  PostprocessingFinancialFeatureModel:
    additionalProperties: false
    properties:
      source:
        description: 'Link(s) to source to validate the given value.

          This is usually used to validate a `y` (documentation link) or `dev` (issue
          or pull request link) but can feasibly be used to validate an `n`.'
        items:
          anyOf:
          - format: uri
            maxLength: 2083
            minLength: 1
            type: string
          - additionalProperties:
              format: uri
              maxLength: 2083
              minLength: 1
              type: string
            maxProperties: 1
            minProperties: 1
            propertyNames:
              enum:
              - levelised_cost
              - net_present_value
              - asset_accounts
              - marginal_price
              - water_value
              - contract_settlement
            type: object
        title: Source
        type: array
      value:
        $ref: '#/$defs/PostprocessingFinancialValueModel'
        default:
          asset_accounts: '?'
          contract_settlement: '?'
          levelised_cost: '?'
          marginal_price: '?'
          net_present_value: '?'
          water_value: '?'
        description: Per-slice feature value.
    title: PostprocessingFinancialFeatureModel
    type: object
  PostprocessingFinancialValueModel:
    additionalProperties: false
    properties:
      asset_accounts:
        default: '?'
        description: Asset-level revenue / cost accounting.
        enum:
        - y
        - n
        - dev
        - '?'
        title: Asset Accounts
        type: string
      contract_settlement:
        default: '?'
        description: Post-hoc settlement of financial contracts against realised dispatch
          and prices, without altering the underlying optimisation (e.g. Contracts-for-difference
          <CfD>, Power/energy purchase agreement <PPA>, Cap-and-floor scheme settlement).
        enum:
        - y
        - n
        - dev
        - '?'
        title: Contract Settlement
        type: string
      levelised_cost:
        default: '?'
        description: Levelised cost of electricity or other carriers.
        enum:
        - y
        - n
        - dev
        - '?'
        title: Levelised Cost
        type: string
      marginal_price:
        default: '?'
        description: Shadow prices extracted via `interface__math.dual_extraction`,
          used to approximate wholesale market prices or as an input to `contract_settlement`
          and soft-constraint interpretation.
        enum:
        - y
        - n
        - dev
        - '?'
        title: Marginal Price
        type: string
      net_present_value:
        default: '?'
        description: Net present value of the modelled system or a subset of assets.
        enum:
        - y
        - n
        - dev
        - '?'
        title: Net Present Value
        type: string
      water_value:
        default: '?'
        description: A future cost function (water value) constructed as a reusable,
          exportable artefact from shadow prices extracted via `interface__math.dual_extraction`
          (e.g. from Benders / SDDP cuts), forecasting the marginal cost of hydro
          and other seasonal storage devices for use in shorter-term scheduling under
          uncertain input parameters.
        enum:
        - y
        - n
        - dev
        - '?'
        title: Water Value
        type: string
    title: PostprocessingFinancialValueModel
    type: object
  PostprocessingImpactAssessmentFeatureModel:
    additionalProperties: false
    properties:
      source:
        description: 'Link(s) to source to validate the given value.

          This is usually used to validate a `y` (documentation link) or `dev` (issue
          or pull request link) but can feasibly be used to validate an `n`.'
        items:
          anyOf:
          - format: uri
            maxLength: 2083
            minLength: 1
            type: string
          - additionalProperties:
              format: uri
              maxLength: 2083
              minLength: 1
              type: string
            maxProperties: 1
            minProperties: 1
            propertyNames:
              enum:
              - emissions
              - resource_use
              - socioeconomic
            type: object
        title: Source
        type: array
      value:
        $ref: '#/$defs/PostprocessingImpactAssessmentValueModel'
        default:
          emissions: '?'
          resource_use: '?'
          socioeconomic: '?'
        description: Per-slice feature value.
    title: PostprocessingImpactAssessmentFeatureModel
    type: object
  PostprocessingImpactAssessmentValueModel:
    additionalProperties: false
    properties:
      emissions:
        default: '?'
        description: GHG or other pollutant emissions.
        enum:
        - y
        - n
        - dev
        - '?'
        title: Emissions
        type: string
      resource_use:
        default: '?'
        description: Non-carrier resource consumption (e.g. water, land, materials).
        enum:
        - y
        - n
        - dev
        - '?'
        title: Resource Use
        type: string
      socioeconomic:
        default: '?'
        description: Socioeconomic impacts (e.g. job creation).
        enum:
        - y
        - n
        - dev
        - '?'
        title: Socioeconomic
        type: string
    title: PostprocessingImpactAssessmentValueModel
    type: object
  PostprocessingModel:
    additionalProperties: false
    properties:
      aggregation:
        $ref: '#/$defs/PostprocessingAggregationFeatureModel'
        default:
          source: []
          value:
            assets: '?'
            spatial: '?'
            temporal: '?'
        description: Method to aggregate across dimensions into broader groups for
          analysis and dissemination This usually requires `interface.dimension_groups`
          to define aggregation membership.
      disaggregation:
        $ref: '#/$defs/PostprocessingDisaggregationFeatureModel'
        default:
          source: []
          value:
            assets: '?'
            spatial: '?'
            temporal: '?'
        description: Model outputs are redistributed back to pre-simplification resolution
          (e.g. capacity-expansion results handed off to a full-resolution power-flow
          model).
      financial:
        $ref: '#/$defs/PostprocessingFinancialFeatureModel'
        default:
          source: []
          value:
            asset_accounts: '?'
            contract_settlement: '?'
            levelised_cost: '?'
            marginal_price: '?'
            net_present_value: '?'
            water_value: '?'
        description: Financial analyses for business cases, derived from model outputs.
      impact_assessment:
        $ref: '#/$defs/PostprocessingImpactAssessmentFeatureModel'
        default:
          source: []
          value:
            emissions: '?'
            resource_use: '?'
            socioeconomic: '?'
        description: Auxiliary impacts calculated from model outputs.
      network_transfer_metrics:
        $ref: '#/$defs/PostprocessingNetworkTransferMetricsFeatureModel'
        default:
          source: []
          value:
            flow_sensitivity: '?'
            transfer_capacity: '?'
        description: Network transfer capability and flow-sensitivity metrics derived
          from model outputs.
      operational:
        $ref: '#/$defs/PostprocessingOperationalFeatureModel'
        default:
          source: []
          value:
            capacity_factor: '?'
            curtailment: '?'
            full_load_hours: '?'
        description: Operational analyses derived from model outputs.
      probabilistic_reliability_assessment:
        $ref: '#/$defs/PostprocessingProbabilisticReliabilityAssessmentFeatureModel'
        default:
          source: []
          value:
            capacity_credit: '?'
            loss_of_load: '?'
            unserved_energy: '?'
        description: Probabilistic resource-adequacy metrics calculated post-hoc.
          Sampling-based run orchestration to produce them is `orchestration.sampled_runs`.
      scenario_comparison:
        $ref: '#/$defs/ToolFeatureModel'
        default:
          source: []
          value: '?'
        description: Outputs of multiple scenario/ensemble runs are collated and compared.
    title: PostprocessingModel
    type: object
  PostprocessingNetworkTransferMetricsFeatureModel:
    additionalProperties: false
    properties:
      source:
        description: 'Link(s) to source to validate the given value.

          This is usually used to validate a `y` (documentation link) or `dev` (issue
          or pull request link) but can feasibly be used to validate an `n`.'
        items:
          anyOf:
          - format: uri
            maxLength: 2083
            minLength: 1
            type: string
          - additionalProperties:
              format: uri
              maxLength: 2083
              minLength: 1
              type: string
            maxProperties: 1
            minProperties: 1
            propertyNames:
              enum:
              - transfer_capacity
              - flow_sensitivity
            type: object
        title: Source
        type: array
      value:
        $ref: '#/$defs/PostprocessingNetworkTransferMetricsValueModel'
        default:
          flow_sensitivity: '?'
          transfer_capacity: '?'
        description: Per-slice feature value.
    title: PostprocessingNetworkTransferMetricsFeatureModel
    type: object
  PostprocessingNetworkTransferMetricsValueModel:
    additionalProperties: false
    properties:
      flow_sensitivity:
        default: '?'
        description: Flow-sensitivity metrics (e.g. power transfer distribution factors,
          line outage distribution factors).
        enum:
        - y
        - n
        - dev
        - '?'
        title: Flow Sensitivity
        type: string
      transfer_capacity:
        default: '?'
        description: Grid transfer capacities between regions or zones.
        enum:
        - y
        - n
        - dev
        - '?'
        title: Transfer Capacity
        type: string
    title: PostprocessingNetworkTransferMetricsValueModel
    type: object
  PostprocessingOperationalFeatureModel:
    additionalProperties: false
    properties:
      source:
        description: 'Link(s) to source to validate the given value.

          This is usually used to validate a `y` (documentation link) or `dev` (issue
          or pull request link) but can feasibly be used to validate an `n`.'
        items:
          anyOf:
          - format: uri
            maxLength: 2083
            minLength: 1
            type: string
          - additionalProperties:
              format: uri
              maxLength: 2083
              minLength: 1
              type: string
            maxProperties: 1
            minProperties: 1
            propertyNames:
              enum:
              - curtailment
              - capacity_factor
              - full_load_hours
            type: object
        title: Source
        type: array
      value:
        $ref: '#/$defs/PostprocessingOperationalValueModel'
        default:
          capacity_factor: '?'
          curtailment: '?'
          full_load_hours: '?'
        description: Per-slice feature value.
    title: PostprocessingOperationalFeatureModel
    type: object
  PostprocessingOperationalValueModel:
    additionalProperties: false
    properties:
      capacity_factor:
        default: '?'
        description: Asset generation relative to full load generation.
        enum:
        - y
        - n
        - dev
        - '?'
        title: Capacity Factor
        type: string
      curtailment:
        default: '?'
        description: Asset generation curtailment.
        enum:
        - y
        - n
        - dev
        - '?'
        title: Curtailment
        type: string
      full_load_hours:
        default: '?'
        description: Number or fraction of timesteps in the model horizon that assets
          are operating at or close their full load.
        enum:
        - y
        - n
        - dev
        - '?'
        title: Full Load Hours
        type: string
    title: PostprocessingOperationalValueModel
    type: object
  PostprocessingProbabilisticReliabilityAssessmentFeatureModel:
    additionalProperties: false
    properties:
      source:
        description: 'Link(s) to source to validate the given value.

          This is usually used to validate a `y` (documentation link) or `dev` (issue
          or pull request link) but can feasibly be used to validate an `n`.'
        items:
          anyOf:
          - format: uri
            maxLength: 2083
            minLength: 1
            type: string
          - additionalProperties:
              format: uri
              maxLength: 2083
              minLength: 1
              type: string
            maxProperties: 1
            minProperties: 1
            propertyNames:
              enum:
              - loss_of_load
              - unserved_energy
              - capacity_credit
            type: object
        title: Source
        type: array
      value:
        $ref: '#/$defs/PostprocessingProbabilisticReliabilityAssessmentValueModel'
        default:
          capacity_credit: '?'
          loss_of_load: '?'
          unserved_energy: '?'
        description: Per-slice feature value.
    title: PostprocessingProbabilisticReliabilityAssessmentFeatureModel
    type: object
  PostprocessingProbabilisticReliabilityAssessmentValueModel:
    additionalProperties: false
    properties:
      capacity_credit:
        default: '?'
        description: Effective load carrying capacity <ELCC>.
        enum:
        - y
        - n
        - dev
        - '?'
        title: Capacity Credit
        type: string
      loss_of_load:
        default: '?'
        description: Loss of load probability/expectation <LOLP>.
        enum:
        - y
        - n
        - dev
        - '?'
        title: Loss Of Load
        type: string
      unserved_energy:
        default: '?'
        description: Expected energy not served <EENS>.
        enum:
        - y
        - n
        - dev
        - '?'
        title: Unserved Energy
        type: string
    title: PostprocessingProbabilisticReliabilityAssessmentValueModel
    type: object
  PreprocessingModel:
    additionalProperties: false
    properties:
      download:
        $ref: '#/$defs/ToolFeatureModel'
        default:
          source: []
          value: '?'
        description: Data can be downloaded directly within the tool from an open
          or proprietary source rather than being specified by the user.
      forecasting:
        $ref: '#/$defs/ToolFeatureModel'
        default:
          source: []
          value: '?'
        description: A user-defined generation / load profile can be automatically
          updated given user-defined forecasting parameters (e.g. change in total
          or peak).
      input_validation:
        $ref: '#/$defs/ToolFeatureModel'
        default:
          source: []
          value: '?'
        description: Input data is validated against the tool's expected structure
          and value ranges before a run, with informative failures.
      model_catalogue:
        $ref: '#/$defs/ToolFeatureModel'
        default:
          source: []
          value: '?'
        description: Pre-built model datasets/examples can be retrieved from a catalogue
          as starting points for new models.
      numeric_scaling:
        $ref: '#/$defs/ToolFeatureModel'
        default:
          source: []
          value: '?'
        description: Data can be scaled (e.g. by converting base units) to reduce
          numeric instability and constraint violations in the downstream solver.
      scenario_generation:
        $ref: '#/$defs/ToolFeatureModel'
        default:
          source: []
          value: '?'
        description: Scenario ensembles can be generated from user-defined parameters
          or sampling rules, for execution as `orchestration.scenario_runs` or `orchestration.sampled_runs`.
      spatial_distribution:
        $ref: '#/$defs/PreprocessingSpatialDistributionFeatureModel'
        default:
          source: []
          value:
            demand: '?'
            network_geometry: '?'
            slack: '?'
        description: A user-defined parameter can be automatically distributed spatially
          using other spatial parameters.
    title: PreprocessingModel
    type: object
  PreprocessingSpatialDistributionFeatureModel:
    additionalProperties: false
    properties:
      source:
        description: 'Link(s) to source to validate the given value.

          This is usually used to validate a `y` (documentation link) or `dev` (issue
          or pull request link) but can feasibly be used to validate an `n`.'
        items:
          anyOf:
          - format: uri
            maxLength: 2083
            minLength: 1
            type: string
          - additionalProperties:
              format: uri
              maxLength: 2083
              minLength: 1
              type: string
            maxProperties: 1
            minProperties: 1
            propertyNames:
              enum:
              - demand
              - network_geometry
              - slack
            type: object
        title: Source
        type: array
      value:
        $ref: '#/$defs/PreprocessingSpatialDistributionValueModel'
        default:
          demand: '?'
          network_geometry: '?'
          slack: '?'
        description: Per-slice feature value.
    title: PreprocessingSpatialDistributionFeatureModel
    type: object
  PreprocessingSpatialDistributionValueModel:
    additionalProperties: false
    properties:
      demand:
        default: '?'
        description: Nodal demand distribution from an aggregate total.
        enum:
        - y
        - n
        - dev
        - '?'
        title: Demand
        type: string
      network_geometry:
        default: '?'
        description: Transmission/pipeline line distances or routing derived from
          node coordinates.
        enum:
        - y
        - n
        - dev
        - '?'
        title: Network Geometry
        type: string
      slack:
        default: '?'
        description: Distributed slack allocation across nodes.
        enum:
        - y
        - n
        - dev
        - '?'
        title: Slack
        type: string
    title: PreprocessingSpatialDistributionValueModel
    type: object
  SystemProcessesModel:
    additionalProperties: false
    properties:
      boundary_exchange:
        $ref: '#/$defs/ToolFeatureModel'
        default:
          source: []
          value: '?'
        description: Carriers enter or leave the system across its boundary at a defined
          cost and availability (e.g. fuel supply, primary resource extraction, imports/exports
          to unmodelled regions).
      buffered_transfer:
        $ref: '#/$defs/ToolFeatureModel'
        default:
          source: []
          value: '?'
        description: 'Flow in space with an endogenous in-transit inventory, to enable
          the flow to accumulate in time (e.g. pipeline linepack).

          '
      conversion:
        $ref: '#/$defs/ToolFeatureModel'
        default:
          source: []
          value: '?'
        description: Carriers are transformed into other carriers within the system
          boundary (e.g. power plants, electrolysers, heat pumps, CO2 capture). This
          enables coupled multi-carrier optimisation.
      delayed_transfer:
        $ref: '#/$defs/ToolFeatureModel'
        default:
          source: []
          value: '?'
        description: Continuous flow in space with a time delay between injection
          and withdrawal (e.g., river flows, district-heating transit).
      discrete_transport:
        $ref: '#/$defs/ToolFeatureModel'
        default:
          source: []
          value: '?'
        description: Carriers are moved in space or exchanged across the system boundary
          in discrete packets by mobile units with loading/unloading decisions (e.g.
          shipping, trucking, rail).
      explicit_demand:
        $ref: '#/$defs/ToolFeatureModel'
        default:
          source: []
          value: '?'
        description: Demand is represented by explicit consuming processes with their
          own constraints, enabling demand-side management. Demanded carriers may
          be non-energy commodities with their own balances.
      spatial_transfer:
        $ref: '#/$defs/ToolFeatureModel'
        default:
          source: []
          value: '?'
        description: Carriers are moved between regions as continuous flow subject
          to transfer-capacity limits, without network physics. See `network__electricity`
          / `network__pipeline` for network physics features.
      state_changing_transfer:
        $ref: '#/$defs/ToolFeatureModel'
        default:
          source: []
          value: '?'
        description: Carrier movement requiring the carrier's physical state to be
          tracked or converted en route (e.g. gas pressure levels with compressors,
          heat-network temperature levels).
      temporal_transfer:
        $ref: '#/$defs/ToolFeatureModel'
        default:
          source: []
          value: '?'
        description: Carriers are moved in time (e.g. storage, demand shifting), linking
          system states across timesteps.
    title: SystemProcessesModel
    type: object
  SystemReliabilityMarginRequirementFeatureModel:
    additionalProperties: false
    properties:
      source:
        description: 'Link(s) to source to validate the given value.

          This is usually used to validate a `y` (documentation link) or `dev` (issue
          or pull request link) but can feasibly be used to validate an `n`.'
        items:
          anyOf:
          - format: uri
            maxLength: 2083
            minLength: 1
            type: string
          - additionalProperties:
              format: uri
              maxLength: 2083
              minLength: 1
              type: string
            maxProperties: 1
            minProperties: 1
            propertyNames:
              enum:
              - capacity
              - operating_reserve
            type: object
        title: Source
        type: array
      value:
        $ref: '#/$defs/SystemReliabilityMarginRequirementValueModel'
        default:
          capacity: '?'
          operating_reserve: '?'
        description: Per-slice feature value.
    title: SystemReliabilityMarginRequirementFeatureModel
    type: object
  SystemReliabilityMarginRequirementValueModel:
    additionalProperties: false
    properties:
      capacity:
        default: '?'
        description: Firm capacity requirement, held against peak demand or a resource-adequacy
          target.
        enum:
        - y
        - n
        - dev
        - '?'
        title: Capacity
        type: string
      operating_reserve:
        default: '?'
        description: Operating reserve requirement, held against short-term contingencies
          (e.g. spinning reserve, ramping reserve).
        enum:
        - y
        - n
        - dev
        - '?'
        title: Operating Reserve
        type: string
    title: SystemReliabilityMarginRequirementValueModel
    type: object
  SystemReliabilityModel:
    additionalProperties: false
    properties:
      contingency_constrained:
        $ref: '#/$defs/ToolFeatureModel'
        default:
          source: []
          value: '?'
        description: The optimisation is constrained to remain feasible following
          the loss of individual components, over an enumerated set of discrete failure
          states (e.g. N-1 security-constrained optimal power flow, loss of a compressor
          station or pipeline segment). Post-contingency corrective action can be
          co-optimised where the tool supports it.
      margin_requirement:
        $ref: '#/$defs/SystemReliabilityMarginRequirementFeatureModel'
        default:
          source: []
          value:
            capacity: '?'
            operating_reserve: '?'
        description: Minimum requirement on firm capacity/margin held in reserve.
          This could be applied at the system level or on single / subsets of assets,
          if `interface.dimension_groups` is available.
      probabilistic_reliability_limit:
        $ref: '#/$defs/SystemReliabilityProbabilisticReliabilityLimitFeatureModel'
        default:
          source: []
          value:
            loss_of_load: '?'
            unserved_energy: '?'
        description: Probabilistic reliability constraints within the optimisation.
      unserved_demand:
        $ref: '#/$defs/ToolFeatureModel'
        default:
          source: []
          value: '?'
        description: Demand can go unserved at a penalty cost (value of lost load
          <VOLL>), placing a soft constraint on load shedding within the optimisation.
    title: SystemReliabilityModel
    type: object
  SystemReliabilityProbabilisticReliabilityLimitFeatureModel:
    additionalProperties: false
    properties:
      source:
        description: 'Link(s) to source to validate the given value.

          This is usually used to validate a `y` (documentation link) or `dev` (issue
          or pull request link) but can feasibly be used to validate an `n`.'
        items:
          anyOf:
          - format: uri
            maxLength: 2083
            minLength: 1
            type: string
          - additionalProperties:
              format: uri
              maxLength: 2083
              minLength: 1
              type: string
            maxProperties: 1
            minProperties: 1
            propertyNames:
              enum:
              - loss_of_load
              - unserved_energy
            type: object
        title: Source
        type: array
      value:
        $ref: '#/$defs/SystemReliabilityProbabilisticReliabilityLimitValueModel'
        default:
          loss_of_load: '?'
          unserved_energy: '?'
        description: Per-slice feature value.
    title: SystemReliabilityProbabilisticReliabilityLimitFeatureModel
    type: object
  SystemReliabilityProbabilisticReliabilityLimitValueModel:
    additionalProperties: false
    properties:
      loss_of_load:
        default: '?'
        description: Loss-of-load-probability <LOLP> limit.
        enum:
        - y
        - n
        - dev
        - '?'
        title: Loss Of Load
        type: string
      unserved_energy:
        default: '?'
        description: Expected-energy-not-served <EENS> limit.
        enum:
        - y
        - n
        - dev
        - '?'
        title: Unserved Energy
        type: string
    title: SystemReliabilityProbabilisticReliabilityLimitValueModel
    type: object
  TimePathwayModel:
    additionalProperties: false
    properties:
      myopic_foresight:
        $ref: '#/$defs/TimePathwayMyopicForesightFeatureModel'
        default:
          source: []
          value:
            investment: '?'
            operation: '?'
        description: Time periods are optimised sequentially, with each decision made
          without knowledge / with limited knowledge of later periods.
      perfect_foresight:
        $ref: '#/$defs/TimePathwayPerfectForesightFeatureModel'
        default:
          source: []
          value:
            investment: '?'
            operation: '?'
        description: Perfect foresight capacity expansion / dispatch.
      singular:
        $ref: '#/$defs/TimePathwaySingularFeatureModel'
        default:
          source: []
          value:
            investment: '?'
            operation: '?'
        description: A single snapshot of a future system can be analysed.
    title: TimePathwayModel
    type: object
  TimePathwayMyopicForesightFeatureModel:
    additionalProperties: false
    properties:
      source:
        description: 'Link(s) to source to validate the given value.

          This is usually used to validate a `y` (documentation link) or `dev` (issue
          or pull request link) but can feasibly be used to validate an `n`.'
        items:
          anyOf:
          - format: uri
            maxLength: 2083
            minLength: 1
            type: string
          - additionalProperties:
              format: uri
              maxLength: 2083
              minLength: 1
              type: string
            maxProperties: 1
            minProperties: 1
            propertyNames:
              enum:
              - investment
              - operation
            type: object
        title: Source
        type: array
      value:
        $ref: '#/$defs/TimePathwayMyopicForesightValueModel'
        default:
          investment: '?'
          operation: '?'
        description: Per-slice feature value.
    title: TimePathwayMyopicForesightFeatureModel
    type: object
  TimePathwayMyopicForesightValueModel:
    additionalProperties: false
    properties:
      investment:
        default: '?'
        description: Investment decisions are optimised sequentially per period /
          set of periods.
        enum:
        - y
        - n
        - dev
        - '?'
        title: Investment
        type: string
      operation:
        default: '?'
        description: Dispatch decisions are optimised sequentially per set of timesteps
          (e.g. days).
        enum:
        - y
        - n
        - dev
        - '?'
        title: Operation
        type: string
    title: TimePathwayMyopicForesightValueModel
    type: object
  TimePathwayPerfectForesightFeatureModel:
    additionalProperties: false
    properties:
      source:
        description: 'Link(s) to source to validate the given value.

          This is usually used to validate a `y` (documentation link) or `dev` (issue
          or pull request link) but can feasibly be used to validate an `n`.'
        items:
          anyOf:
          - format: uri
            maxLength: 2083
            minLength: 1
            type: string
          - additionalProperties:
              format: uri
              maxLength: 2083
              minLength: 1
              type: string
            maxProperties: 1
            minProperties: 1
            propertyNames:
              enum:
              - investment
              - operation
            type: object
        title: Source
        type: array
      value:
        $ref: '#/$defs/TimePathwayPerfectForesightValueModel'
        default:
          investment: '?'
          operation: '?'
        description: Per-slice feature value.
    title: TimePathwayPerfectForesightFeatureModel
    type: object
  TimePathwayPerfectForesightValueModel:
    additionalProperties: false
    properties:
      investment:
        default: '?'
        description: All investment periods are optimised simultaneously with perfect
          foresight of future periods.
        enum:
        - y
        - n
        - dev
        - '?'
        title: Investment
        type: string
      operation:
        default: '?'
        description: All dispatch periods are optimised simultaneously with perfect
          foresight of future timesteps in the investment period(s).
        enum:
        - y
        - n
        - dev
        - '?'
        title: Operation
        type: string
    title: TimePathwayPerfectForesightValueModel
    type: object
  TimePathwaySingularFeatureModel:
    additionalProperties: false
    properties:
      source:
        description: 'Link(s) to source to validate the given value.

          This is usually used to validate a `y` (documentation link) or `dev` (issue
          or pull request link) but can feasibly be used to validate an `n`.'
        items:
          anyOf:
          - format: uri
            maxLength: 2083
            minLength: 1
            type: string
          - additionalProperties:
              format: uri
              maxLength: 2083
              minLength: 1
              type: string
            maxProperties: 1
            minProperties: 1
            propertyNames:
              enum:
              - investment
              - operation
            type: object
        title: Source
        type: array
      value:
        $ref: '#/$defs/TimePathwaySingularValueModel'
        default:
          investment: '?'
          operation: '?'
        description: Per-slice feature value.
    title: TimePathwaySingularFeatureModel
    type: object
  TimePathwaySingularValueModel:
    additionalProperties: false
    properties:
      investment:
        default: '?'
        description: '"Overnight" capacity expansion, focussing on the end-state design
          of a system without considering how that design is reached.'
        enum:
        - y
        - n
        - dev
        - '?'
        title: Investment
        type: string
      operation:
        default: '?'
        description: Single snapshot operation dispatch, i.e. allowing optimisation
          without a time dimension.
        enum:
        - y
        - n
        - dev
        - '?'
        title: Operation
        type: string
    title: TimePathwaySingularValueModel
    type: object
  ToolFeatureModel:
    additionalProperties: false
    description: Feature-level fields.
    properties:
      source:
        description: 'Link(s) to source to validate the given value.

          This is usually used to validate a `y` (documentation link) or `dev` (issue
          or pull request link) but can feasibly be used to validate an `n`.'
        items:
          format: uri
          maxLength: 2083
          minLength: 1
          type: string
        title: Source
        type: array
      value:
        default: '?'
        description: 'Whether a feature exists (`y`) or not (`n`), or is in development
          (`dev`) in the tool.

          Defaults to unknown (`?`).'
        enum:
        - y
        - n
        - dev
        - '?'
        title: Value
        type: string
    title: ToolFeatureModel
    type: object
  TractabilityDimensionReductionAssetsFeatureModel:
    additionalProperties: false
    properties:
      source:
        description: 'Link(s) to source to validate the given value.

          This is usually used to validate a `y` (documentation link) or `dev` (issue
          or pull request link) but can feasibly be used to validate an `n`.'
        items:
          anyOf:
          - format: uri
            maxLength: 2083
            minLength: 1
            type: string
          - additionalProperties:
              format: uri
              maxLength: 2083
              minLength: 1
              type: string
            maxProperties: 1
            minProperties: 1
            propertyNames:
              enum:
              - clustering
              - black_boxing
              - component_removal
            type: object
        title: Source
        type: array
      value:
        $ref: '#/$defs/TractabilityDimensionReductionAssetsValueModel'
        default:
          black_boxing: '?'
          clustering: '?'
          component_removal: '?'
        description: Per-slice feature value.
    title: TractabilityDimensionReductionAssetsFeatureModel
    type: object
  TractabilityDimensionReductionAssetsValueModel:
    additionalProperties: false
    properties:
      black_boxing:
        default: '?'
        description: A chain of interconnected assets is collapsed into a single component
          with an in/out interface. This usually requires `asset__constraints.dedicated_flow_routing`
          between interconnected components.
        enum:
        - y
        - n
        - dev
        - '?'
        title: Black Boxing
        type: string
      clustering:
        default: '?'
        description: Individually defined assets are automatically aggregated into
          asset groups for optimisation. This usually requires `interface.dimension_groups`
          to define aggregation membership.
        enum:
        - y
        - n
        - dev
        - '?'
        title: Clustering
        type: string
      component_removal:
        default: '?'
        description: Individual or groups of assets (e.g. sharing a node or a carrier)
          can be systematically filtered out of the built problem.
        enum:
        - y
        - n
        - dev
        - '?'
        title: Component Removal
        type: string
    title: TractabilityDimensionReductionAssetsValueModel
    type: object
  TractabilityDimensionReductionModel:
    additionalProperties: false
    properties:
      assets:
        $ref: '#/$defs/TractabilityDimensionReductionAssetsFeatureModel'
        default:
          source: []
          value:
            black_boxing: '?'
            clustering: '?'
            component_removal: '?'
        description: Asset resolution adjustment options.
      spatial:
        $ref: '#/$defs/TractabilityDimensionReductionSpatialFeatureModel'
        default:
          source: []
          value:
            clustering: '?'
            network_reduction: '?'
        description: Spatial resolution adjustment options.
      temporal:
        $ref: '#/$defs/TractabilityDimensionReductionTemporalFeatureModel'
        default:
          source: []
          value:
            dimension_non_contiguity: '?'
            load_duration_curves: '?'
            resampling: '?'
            typical_periods: '?'
        description: Temporal resolution adjustment options (may apply to within-year
          timesteps or between-year investment periods).
    title: TractabilityDimensionReductionModel
    type: object
  TractabilityDimensionReductionSpatialFeatureModel:
    additionalProperties: false
    properties:
      source:
        description: 'Link(s) to source to validate the given value.

          This is usually used to validate a `y` (documentation link) or `dev` (issue
          or pull request link) but can feasibly be used to validate an `n`.'
        items:
          anyOf:
          - format: uri
            maxLength: 2083
            minLength: 1
            type: string
          - additionalProperties:
              format: uri
              maxLength: 2083
              minLength: 1
              type: string
            maxProperties: 1
            minProperties: 1
            propertyNames:
              enum:
              - clustering
              - network_reduction
            type: object
        title: Source
        type: array
      value:
        $ref: '#/$defs/TractabilityDimensionReductionSpatialValueModel'
        default:
          clustering: '?'
          network_reduction: '?'
        description: Per-slice feature value.
    title: TractabilityDimensionReductionSpatialFeatureModel
    type: object
  TractabilityDimensionReductionSpatialValueModel:
    additionalProperties: false
    properties:
      clustering:
        default: '?'
        description: Automatic spatial zone aggregation/disaggregation. Usually from
          nodal (bus) level to regional/bidding zone level. This usually requires
          `interface.dimension_groups` to define aggregation membership.
        enum:
        - y
        - n
        - dev
        - '?'
        title: Clustering
        type: string
      network_reduction:
        default: '?'
        description: Network element equivalencing (e.g., reducing to a single voltage
          level).
        enum:
        - y
        - n
        - dev
        - '?'
        title: Network Reduction
        type: string
    title: TractabilityDimensionReductionSpatialValueModel
    type: object
  TractabilityDimensionReductionTemporalFeatureModel:
    additionalProperties: false
    properties:
      source:
        description: 'Link(s) to source to validate the given value.

          This is usually used to validate a `y` (documentation link) or `dev` (issue
          or pull request link) but can feasibly be used to validate an `n`.'
        items:
          anyOf:
          - format: uri
            maxLength: 2083
            minLength: 1
            type: string
          - additionalProperties:
              format: uri
              maxLength: 2083
              minLength: 1
              type: string
            maxProperties: 1
            minProperties: 1
            propertyNames:
              enum:
              - resampling
              - typical_periods
              - load_duration_curves
              - dimension_non_contiguity
            type: object
        title: Source
        type: array
      value:
        $ref: '#/$defs/TractabilityDimensionReductionTemporalValueModel'
        default:
          dimension_non_contiguity: '?'
          load_duration_curves: '?'
          resampling: '?'
          typical_periods: '?'
        description: Per-slice feature value.
    title: TractabilityDimensionReductionTemporalFeatureModel
    type: object
  TractabilityDimensionReductionTemporalValueModel:
    additionalProperties: false
    properties:
      dimension_non_contiguity:
        default: '?'
        description: Mathematical constraints to manage inter-temporal model features
          when using non-contiguous temporal aggregation (e.g., linking storage levels
          across typical periods).
        enum:
        - y
        - n
        - dev
        - '?'
        title: Dimension Non Contiguity
        type: string
      load_duration_curves:
        default: '?'
        description: Full timeseries is represented by load duration curves.
        enum:
        - y
        - n
        - dev
        - '?'
        title: Load Duration Curves
        type: string
      resampling:
        default: '?'
        description: Temporal resolution of the input timeseries is coarsened while
          remaining contiguous (e.g. hourly to daily) before the model is built.
        enum:
        - y
        - n
        - dev
        - '?'
        title: Resampling
        type: string
      typical_periods:
        default: '?'
        description: Full timeseries is represented by non-contiguous typical periods
          (days, weeks, months).
        enum:
        - y
        - n
        - dev
        - '?'
        title: Typical Periods
        type: string
    title: TractabilityDimensionReductionTemporalValueModel
    type: object
  TractabilityReformulationModel:
    additionalProperties: false
    properties:
      approximation:
        $ref: '#/$defs/ToolFeatureModel'
        default:
          source: []
          value: '?'
        description: The solution algorithm is reformulated to reach a near-optimal
          answer within tolerance at reduced cost (e.g. progressive hedging, Stochastic
          Dual Dynamic Programming <SDDP>-style cut selection).
      constraint_screening:
        $ref: '#/$defs/ToolFeatureModel'
        default:
          source: []
          value: '?'
        description: Constraints unlikely to bind are omitted or lazily added.
      convex_relaxation:
        $ref: '#/$defs/ToolFeatureModel'
        default:
          source: []
          value: '?'
        description: A non-convex relationship is approximated by a convex relaxation,
          retaining solver tractability (e.g. second-order-cone relaxation of AC power
          flow).
      decomposition:
        $ref: '#/$defs/ToolFeatureModel'
        default:
          source: []
          value: '?'
        description: The problem is split into coordinated sub-problems solved iteratively
          or in parallel until convergence (e.g. Benders decomposition; Stochastic
          Dual Dynamic Programming <SDDP> for multi-stage stochastic hydropower scheduling).
      function_conversion:
        $ref: '#/$defs/ToolFeatureModel'
        default:
          source: []
          value: '?'
        description: User-defined arbitrary non-linear functions are automatically
          converted into valid model inputs (e.g. piecewise segments) without manual
          discretisation.
      linearised_unit_commitment:
        $ref: '#/$defs/ToolFeatureModel'
        default:
          source: []
          value: '?'
        description: Unit commitment approximated with continuous (linearised) commitment
          variables, avoiding integer decision variables.
      variable_fidelity:
        $ref: '#/$defs/ToolFeatureModel'
        default:
          source: []
          value: '?'
        description: The mathematical fidelity of the same physical attribute can
          differ across the model's scope within one model definition (e.g. discrete
          units in some investment periods and aggregated capacity in others; unit
          commitment over the near-term horizon, continuous economic dispatch beyond).
    title: TractabilityReformulationModel
    type: object
  UncertaintyModel:
    additionalProperties: false
    properties:
      multi_stage_stochastic:
        $ref: '#/$defs/ToolFeatureModel'
        default:
          source: []
          value: '?'
        description: Multi-stage stochastic optimisation, in which decisions are revised
          as uncertainty resolves stage by stage. Primarily used for scheduling seasonal
          storage (e.g. hydropower water values) under uncertain inflows.
      near_optimal:
        $ref: '#/$defs/ToolFeatureModel'
        default:
          source: []
          value: '?'
        description: Generate a near-optimal solution set by relaxing constraints
          / objectives in the optimisation problem. (e.g., Modelling to Generate Alternatives
          <MGA>).
      risk_aversion:
        $ref: '#/$defs/ToolFeatureModel'
        default:
          source: []
          value: '?'
        description: Risk measures (e.g. Conditional Value at Risk <CVaR>) applied
          to a stochastic optimisation as chance constraints. It weights tail-end
          outcomes according to a configurable risk tolerance.
      robust_optimisation:
        $ref: '#/$defs/ToolFeatureModel'
        default:
          source: []
          value: '?'
        description: Worst-case cost minimisation over an uncertainty set.
      two_stage_stochastic:
        $ref: '#/$defs/ToolFeatureModel'
        default:
          source: []
          value: '?'
        description: Two-stage stochastic optimisation; used in investment decision
          making under uncertainty.
    title: UncertaintyModel
    type: object
  WorkflowTransparencyMetadataPropagationFeatureModel:
    additionalProperties: false
    properties:
      source:
        description: 'Link(s) to source to validate the given value.

          This is usually used to validate a `y` (documentation link) or `dev` (issue
          or pull request link) but can feasibly be used to validate an `n`.'
        items:
          anyOf:
          - format: uri
            maxLength: 2083
            minLength: 1
            type: string
          - additionalProperties:
              format: uri
              maxLength: 2083
              minLength: 1
              type: string
            maxProperties: 1
            minProperties: 1
            propertyNames:
              enum:
              - provenance
              - units
            type: object
        title: Source
        type: array
      value:
        $ref: '#/$defs/WorkflowTransparencyMetadataPropagationValueModel'
        default:
          provenance: '?'
          units: '?'
        description: Per-slice feature value.
    title: WorkflowTransparencyMetadataPropagationFeatureModel
    type: object
  WorkflowTransparencyMetadataPropagationValueModel:
    additionalProperties: false
    properties:
      provenance:
        default: '?'
        description: Data sources, versions, and licences are propagated from inputs
          through to outputs.
        enum:
        - y
        - n
        - dev
        - '?'
        title: Provenance
        type: string
      units:
        default: '?'
        description: Physical units attached to data are tracked and validated through
          the pipeline (to enable e.g., unit-consistency checking, automatic conversion,
          auto-scaling).
        enum:
        - y
        - n
        - dev
        - '?'
        title: Units
        type: string
    title: WorkflowTransparencyMetadataPropagationValueModel
    type: object
  WorkflowTransparencyModel:
    additionalProperties: false
    properties:
      infeasibility_diagnosis:
        $ref: '#/$defs/ToolFeatureModel'
        default:
          source: []
          value: '?'
        description: Infeasible problems can be diagnosed (e.g. irreducible infeasible
          subset (IIS) generation, named constraint violations).
      intermediate_access:
        $ref: '#/$defs/ToolFeatureModel'
        default:
          source: []
          value: '?'
        description: Intermediate pipeline artefacts (e.g. built problem data, solver
          state) can be exposed to the user.
      metadata_propagation:
        $ref: '#/$defs/WorkflowTransparencyMetadataPropagationFeatureModel'
        default:
          source: []
          value:
            provenance: '?'
            units: '?'
        description: Metadata is propagated from inputs through to outputs.
      run_logs:
        $ref: '#/$defs/ToolFeatureModel'
        default:
          source: []
          value: '?'
        description: Run logs available (e.g. in text files), for use in debugging
          failing model runs.
    title: WorkflowTransparencyModel
    type: object
additionalProperties: false
properties:
  features:
    $ref: '#/$defs/FeatureSetModel'
    default:
      actors:
        distributed_objectives:
          source: []
          value: '?'
        equilibrium:
          source: []
          value: '?'
        portfolio:
          source: []
          value: '?'
      asset__capacity_representation:
        continuous_capacity:
          source: []
          value: '?'
        exogenous_capacity:
          source: []
          value: '?'
        integer_investment_continuous_capacity:
          source: []
          value: '?'
        integer_investment_exogenous_capacity:
          source: []
          value: '?'
      asset__characteristics:
        configurable_dimensions:
          source: []
          value:
            investment: '?'
            operation: '?'
        extensible_dimensions:
          source: []
          value:
            investment: '?'
            operation: '?'
        linear:
          source: []
          value:
            investment: '?'
            operation: '?'
        nonlinear:
          source: []
          value:
            investment: '?'
            operation: '?'
        path_dependent:
          source: []
          value:
            investment: '?'
            operation: '?'
        quadratic:
          source: []
          value:
            investment: '?'
            operation: '?'
      asset__constraints:
        boundary_conditions:
          source: []
          value:
            investment: '?'
            operation: '?'
        commitment:
          source: []
          value:
            investment: '?'
            operation: '?'
        dedicated_flow_routing:
          source: []
          value: '?'
        inter_asset:
          source: []
          value:
            investment: '?'
            operation: '?'
        inter_spatial:
          source: []
          value:
            investment: '?'
            operation: '?'
        inter_temporal:
          source: []
          value:
            investment: '?'
            operation: '?'
        intra_temporal:
          source: []
          value:
            investment: '?'
            operation: '?'
        total:
          source: []
          value:
            investment: '?'
            operation: '?'
      asset__cost:
        annuitisation:
          source: []
          value: '?'
        inter_temporal:
          source: []
          value:
            investment: '?'
            operation: '?'
        linear:
          source: []
          value:
            investment: '?'
            operation: '?'
        nonlinear:
          source: []
          value:
            investment: '?'
            operation: '?'
        threshold_deviation:
          source: []
          value:
            investment: '?'
            operation: '?'
        unit:
          source: []
          value:
            investment: '?'
            operation: '?'
      asset__operating_representation:
        maintenance_scheduling:
          source: []
          value: '?'
        unit_commitment:
          source: []
          value: '?'
      data__io:
        binary:
          source: []
          value:
            input: '?'
            output: '?'
        database:
          source: []
          value:
            input: '?'
            output: '?'
        lazy_loading:
          source: []
          value:
            input: '?'
            output: '?'
        plain_text:
          source: []
          value:
            input: '?'
            output: '?'
        remote_storage:
          source: []
          value:
            input: '?'
            output: '?'
      edge_effects:
        energy_balance:
          source: []
          value: '?'
        time_financial:
          source: []
          value: '?'
        time_operational:
          source: []
          value: '?'
      interface:
        analysis_templates:
          source: []
          value: '?'
        api:
          source: []
          value:
            analyse: '?'
            build: '?'
            run: '?'
        cli:
          source: []
          value:
            analyse: '?'
            build: '?'
            run: '?'
        component_templating:
          source: []
          value: '?'
        dimension_groups:
          source: []
          value: '?'
        gui:
          source: []
          value:
            analyse: '?'
            build: '?'
            run: '?'
        remote_execution:
          source: []
          value: '?'
        solver_configuration:
          source: []
          value: '?'
        standardised_data:
          source: []
          value:
            analyse: '?'
            build: '?'
      interface__math:
        dual_extraction:
          source: []
          value: '?'
        formulation_transparency:
          source: []
          value: '?'
        piecewise_formulation:
          source: []
          value: '?'
        problem_file_export:
          source: []
          value: '?'
        soft_constraints:
          source: []
          value: '?'
        user_defined_math:
          source: []
          value: '?'
      network__electricity:
        network_devices:
          source: []
          value:
            facts_devices: '?'
            hvdc_converters: '?'
            transformers: '?'
        network_limits:
          source: []
          value:
            phase_angle: '?'
            thermal: '?'
        optimal_power_flow:
          source: []
          value:
            ac_opf: '?'
            dc_opf: '?'
        sensitivity_limits:
          source: []
          value:
            generic_constraints: '?'
            outage_factors: '?'
            ptdf: '?'
        unbalanced_flows:
          source: []
          value: '?'
      network__pipeline:
        composition_tracking:
          source: []
          value: '?'
        linearised_flow:
          source: []
          value: '?'
      optimisation__objective:
        multi_objective_pareto:
          source: []
          value: '?'
        multi_objective_weighted:
          source: []
          value: '?'
        quadratic:
          source: []
          value: '?'
      orchestration:
        rolling_horizon:
          source: []
          value: '?'
        sampled_runs:
          source: []
          value: '?'
        scenario_runs:
          source: []
          value: '?'
        simulation_optimisation:
          source: []
          value: '?'
      postprocessing:
        aggregation:
          source: []
          value:
            assets: '?'
            spatial: '?'
            temporal: '?'
        disaggregation:
          source: []
          value:
            assets: '?'
            spatial: '?'
            temporal: '?'
        financial:
          source: []
          value:
            asset_accounts: '?'
            contract_settlement: '?'
            levelised_cost: '?'
            marginal_price: '?'
            net_present_value: '?'
            water_value: '?'
        impact_assessment:
          source: []
          value:
            emissions: '?'
            resource_use: '?'
            socioeconomic: '?'
        network_transfer_metrics:
          source: []
          value:
            flow_sensitivity: '?'
            transfer_capacity: '?'
        operational:
          source: []
          value:
            capacity_factor: '?'
            curtailment: '?'
            full_load_hours: '?'
        probabilistic_reliability_assessment:
          source: []
          value:
            capacity_credit: '?'
            loss_of_load: '?'
            unserved_energy: '?'
        scenario_comparison:
          source: []
          value: '?'
      preprocessing:
        download:
          source: []
          value: '?'
        forecasting:
          source: []
          value: '?'
        input_validation:
          source: []
          value: '?'
        model_catalogue:
          source: []
          value: '?'
        numeric_scaling:
          source: []
          value: '?'
        scenario_generation:
          source: []
          value: '?'
        spatial_distribution:
          source: []
          value:
            demand: '?'
            network_geometry: '?'
            slack: '?'
      system__processes:
        boundary_exchange:
          source: []
          value: '?'
        buffered_transfer:
          source: []
          value: '?'
        conversion:
          source: []
          value: '?'
        delayed_transfer:
          source: []
          value: '?'
        discrete_transport:
          source: []
          value: '?'
        explicit_demand:
          source: []
          value: '?'
        spatial_transfer:
          source: []
          value: '?'
        state_changing_transfer:
          source: []
          value: '?'
        temporal_transfer:
          source: []
          value: '?'
      system__reliability:
        contingency_constrained:
          source: []
          value: '?'
        margin_requirement:
          source: []
          value:
            capacity: '?'
            operating_reserve: '?'
        probabilistic_reliability_limit:
          source: []
          value:
            loss_of_load: '?'
            unserved_energy: '?'
        unserved_demand:
          source: []
          value: '?'
      time__pathway:
        myopic_foresight:
          source: []
          value:
            investment: '?'
            operation: '?'
        perfect_foresight:
          source: []
          value:
            investment: '?'
            operation: '?'
        singular:
          source: []
          value:
            investment: '?'
            operation: '?'
      tractability__dimension_reduction:
        assets:
          source: []
          value:
            black_boxing: '?'
            clustering: '?'
            component_removal: '?'
        spatial:
          source: []
          value:
            clustering: '?'
            network_reduction: '?'
        temporal:
          source: []
          value:
            dimension_non_contiguity: '?'
            load_duration_curves: '?'
            resampling: '?'
            typical_periods: '?'
      tractability__reformulation:
        approximation:
          source: []
          value: '?'
        constraint_screening:
          source: []
          value: '?'
        convex_relaxation:
          source: []
          value: '?'
        decomposition:
          source: []
          value: '?'
        function_conversion:
          source: []
          value: '?'
        linearised_unit_commitment:
          source: []
          value: '?'
        variable_fidelity:
          source: []
          value: '?'
      uncertainty:
        multi_stage_stochastic:
          source: []
          value: '?'
        near_optimal:
          source: []
          value: '?'
        risk_aversion:
          source: []
          value: '?'
        robust_optimisation:
          source: []
          value: '?'
        two_stage_stochastic:
          source: []
          value: '?'
      workflow__transparency:
        infeasibility_diagnosis:
          source: []
          value: '?'
        intermediate_access:
          source: []
          value: '?'
        metadata_propagation:
          source: []
          value:
            provenance: '?'
            units: '?'
        run_logs:
          source: []
          value: '?'
    description: Tool feature set.
title: ToolFeatureModel
type: object
